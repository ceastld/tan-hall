# 弹堂 · TAN HALL — 完整游戏设计文档

> 本文是实现用细则。程序员按数字做，不要猜，不要从别的弹道对决游戏抄名字。  
> v0.1 短案仍在 [DESIGN.md](./DESIGN.md)，手感验收在 [PLAYTEST.md](./PLAYTEST.md)。  
> 冲突时：已上架行为以 `js/game.js` 常量为准；本文规划段（标「v0.2+」）覆盖尚未写的系统。  
> 禁止出现的词：传送、飞行、三叉戟、激怒，以及任何 TNT / 弹弹堂 / 弹弹星球的角色、武器、地图、UI 原文。  
> **v0.3 已上（fun slice）**：镜头跟弹 + 命中 80–120ms 顿帧与 punch；落地后缓回双方同框。新枪 **三裂**（键 4，落地三坑叠埋）。新图 **风柱**（中央上升气流）与 **碎桥**（薄桥可被高爆/三裂打断）。坑深 ≥40px 吐司「埋了」，不能走出只能飞步/影挪。瞄准期 HUD 一行建议角 `65±2×风` 与 65° 表力度，不代打。  
> **v0.4 已上（fun slice）**：**殿塌**（第 12 回合后，或双方 HP≤35：两侧塌方、安全带内收，吐司「殿塌」）。烬丸有人味：65° 力抖 5–8%、偶飞步、台沿三裂埋、坑里影挪、可见蓄力、稀有吐司「烬丸冷笑」「烬丸补角」。拖尾加粗、着土、HP 弹出、直击 100ms 慢镜、殿破更响。新枪 **霓轨**（键 5，风乘 1.15）。新图 **悬岛**（三块浮岛，坠隙即死）。辅助瞄准仍是 `G.assist` 0–3（H / 辅，默认 2）。  
> **v0.5 已上（fun slice）**：新枪 **霓火**（键 6，坑 20 弱于高爆，落地火潭半径 28，两回合 settle 开始烧 8，殿破点燃 ×1.6）。新图 **残垣**（两堵残墙作掩体，高爆 / 三裂可打洞）。超时钟末 5 秒脉动，超时仍半力打出。殿塌碎屑落地扬尘。  
> **v0.6 已上（fun slice）**：本机 **对坐**（标题钮 / 键 4）。两人共用键盘，岚丸先手再烬丸，无 AI、无联网。规则同弹堂（HP 100、风 −8～+8、道具/体力/怒/辅助都给当前行动者）。HUD「岚丸的回合」/「烬丸的回合」。落地后镜头缓到下一名射手。胜吐司写「岚丸胜」/「烬丸胜」。  
> **v0.7 已上（fun slice）**：新图 **风谷** `vale`（深 V + 中路强升流，峰值约 −240，强于风柱 −150，65° 高抛被扬起）与 **熔台** `forge`（两侧高台 + 薄壳覆熔坑，坠入即死，教 90° 砸壳与飞步过缝）。标题页九图。上回残影保持。  
> **v0.8 已上（fun slice）**：弹道中 **殿果**（弹堂 / 堂核 / 对坐，演习场不刷）。每发 0–2 颗悬浮灵珠，开局第一回合不刷；弹心碰到即拾，随机 +1 飞步 / 影挪 / 霓弹 / 鼓息（上限为开局次数 +1），约 15% 金殿果一次 +25 怒。未击中的果在本发 settle 后消失。烬丸弹道碰到也会拾，不专门找。辅助 中 / 强落点叉圈轻脉动（`prefers-reduced-motion` 不闪）。  
> **v0.9 已上（fun slice）**：**对堂**（标题钮 / 键 5）。2v2 延迟出手：左 岚丸 + 霜丸 vs 右 烬丸 + 霆丸。行动后 delay += 100 + 武器额外（高爆 +30、三裂 +20、霓轨 +40、殿破 +20）；下一名 = 最低 delay。飞步 / 影挪 / 鼓息不加 delay、不结束回合。双方都倒下才胜。对坐仍是 1v1 人类，弹堂仍是 1v1 vs AI。  
> **v1.2 已上**：**对堂**左队双控。延迟队列切人，玩家寄身当前左队单位（岚丸青焰 / 霜丸冰焰）。霜丸无 AI。右队烬丸 + 霆丸仍 AI。吐司「岚丸出手」/「霜丸出手」。道具 / 体 / 怒 / 辅助跟当前寄身。无 T 换人。  
> **v1.3 已上**：新图 **廊桥** `arcade`（两侧高岸 + 中央长拱石桥，厚可站、薄可被高爆 / 三裂砸穿；坠洞即死）。教学 65° 越拱 vs 30° 穿拱。出生在岸上，拱是争夺位。对堂四人仍错开在两岸。随图含廊桥。  
> **v1.4 已上**：**障幕**（键 G，每局 1，−20 体）。在当前单位落下半径 70px 青雾盘，持续到 **下一次敌方行动** 结束（对堂延迟：队友出手不消耗，敌方一次行动后消散）。雾内：对方辅助落点叉圈隐藏；AI 瞄准误差 +12%；自己仍看得见。画布旋涡青雾，无 DOM 层、`pointer-events` 不挡。吐司「障幕」。AI：HP < 40 且 |距| < 8 格才用。不进殿果。  
> **v1.5 已上**：新图 **双塔** `towers`（左右粗石塔，内沿相距约 180px；平顶可站、内沿有巢台；**高爆 / 三裂**啃垛口，**穿透**开窗）。坠塔只吃摔伤，无熔浆；对堂额外两人站内沿巢台。教学 90° 砸顶与 65° 对射。标题十一图，随图含双塔。  
> **v1.6 已上**：**堂座**（标题钮 / 键 6）。同对堂 2v2 延迟，两侧都是人类热座：玩家甲寄身岚丸+霜丸，乙寄身烬丸+霆丸。换边大吐司「把键盘给烬丸」/「把键盘给岚丸」（按边，不写霜丸/霆丸）。辅助跟当前寄身。无 AI。对堂仍是左队玩家 vs 右队 AI。  
> **v1.7 已上**：直击顿帧 **140ms** + 更狠镜头 punch；埋了土喷泉。弹堂首局前三发小吐司「看风」「65° 最远」「高抛埋人」，写入 `best.coached`，堂座 / 对堂不教。击破慢镜 220ms 后定格名字。无挡 R 的教程层。  
> **v1.8 已上**：**烬丸难度** 三档（标题 易 / 中 / 狠，写入 `best.ai`，默认 **中**=1）。易：角抖 ±8、力抖 ±6，少用术，HP≥30 不开殿破，从不用障幕。中：现人味（弹堂 ±3°/±4 + 65° 力抖 5–8%，堂核 ±1.5/±2）。狠：角力抖 ±2，贪三裂埋、逆息、障幕、殿破；对堂用低延迟枪抢两手。HUD「烬 · 中」。堂座无 AI 不吃档；对堂右队吃档。  
> **v1.9 已上**：新图 **月池** `moon`（中央圆坑湖；坑内慢走 0.45×，回合开始若仍在水里伤 3；沿高、对沿 65°；高爆可把沿块塌进池）。标题十二图，随图含月池。  
> **v2.0 已上**：**上回弹道残影**。任意一发落地记下全长折线；下一单位瞄准（所有人）画金色 30% 虚线；HUD「上 65°/70」；弹在飞时不画（本发拖尾）；只活本局；K 开关，默认开，`best.ghost`。  
> **v2.1 已上**：新图 **断崖** `cliff`（左高台、右低滩、中段约 180px 绝壁）。台沿 90° 砸滩可埋；滩上 30° 可掏壁。坠崖吃摔伤，非即死；崖底浅水每回合 2（弱于月池 3）。出生左台 / 右滩，对堂额外同面。标题十三图，随图含断崖。  
> **v2.2 已上**：**风丝**。天空横向风丝，条数 / 速度随 `|风|` 缩放，方向跟风号；`|风|≥5` 更粗更快。近地可选叶尘。`prefers-reduced-motion` 藏丝，顶栏数字仍在。只画不改物理。断崖与上回残影 K 不改。  
> **v2.3 已上**：新图 **沙脊** `dune`（两侧高丘、中央浅鞍）。出生 168 / 772 在丘顶。沙地慢走 **0.55×**；弹坑约 **×1.25**。台沿 **90°** 砸鞍可埋；**30°** 可掏对丘。本图风掷范围 ±1（仍夹 `windMax()`）。对堂额外同丘面。标题十四图，随图含沙脊。风丝 / 残影 K / 断崖 / 月池不改。  
> **v2.4 已上**：**叠珠**（键 7，武器 7）。一发两壳：第一发蓄力，0.38s 后第二发 0.72× 力、同角同膛、±1.2° 微抖，溅射/坑亦 ×0.72。每壳独立碰/伤/挖。不能第三发；第一发已结束对局则不刷第二发。AI 对方埋了或在坑里用叠珠收尾。HUD 名 **叠珠**，短提示「两发」。十四图 / 风丝 / K 残影 / 沙脊 / 断崖不改。`GRAV` 260、`VK` 420 不动。
> **v2.5 已上**：**雷泽**。天气系统（不是传送、不是飞行）。**风谷 / 断崖 / 沙脊** 开局必开；**熔台** 永不开；其余图约 35%。天空略暗，画布闪雷 80–160ms、雨丝；`prefers-reduced-motion` 藏闪与雨，HUD「雷泽」仍在。对局每 8–14s（overlay 打开时不算）风可跳 ±1，仍夹 `windMax()`。湿沙/草走 ×0.88；飞步/影挪不变。不改图名。`GRAV` 260、`VK` 420 不动。
> **v2.6 已上**：新图 **石门** `gate`。两座石柱（左约 x 280，右约 x 660）中缝廊道；出生 168 / 772 在外沿土台。**30°** 可穿缝，**90°** 砸柱冠，**65°** 常削柱沿。柱石坑半径 **×0.72**（按 x 带 + 图判定），土台仍原坑。冠上满速，坠入廊可埋。对堂额外同外沿，不进门缝。AI：敌在廊里偏 90° / 高爆；敌在对冠偏 65°。雷泽约 35%（非必开）。叠珠 / 风丝 / K 残影 / 断崖 / 沙脊不改。`GRAV` 260、`VK` 420 不动。标题十五图。
> **v2.7 已上**：**迟雷**（键 8，武器 8）。一发着地粘住，**1.6s** 后爆（溅射 ~42、坑 ~34，可埋）。飞行中先碰人则立刻爆，溅射 **×0.85**，不粘。每边最多一枚；再打替换，旧雷熄、不爆。画布倒计时环 + 脉动标；减动效静标不闪。AI：敌将走进石门廊 / 沙脊鞍时封路，不当开局枪。1–7 / 十五图 / 雷泽 / 叠珠 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒。
> **v2.8 已上**：新图 **霜泽** `frost`。两侧土岸（出生 168 / 772）+ 中央宽平冻湖。冰上走 **1.15×** 且滑（松手仍带残速，难停）；岸上正常。弹打冰、着角 **< 28°** 跳一次（`vy * 0.55` 反弹、`vx * 0.92`，小火花），第二次才爆；陡击照常爆。冰坑 **×0.55**，岸上原坑。对堂额外同岸。AI：敌在湖上偏 65°/90° 砸冰（不跳）；岸唇 **迟雷** 封退路。雷泽约 35%。叠珠 / 迟雷 / 风丝 / K 残影 / 断崖 / 沙脊 / 石门不改。`GRAV` 260、`VK` 420。标题十六图。
> **v2.9 已上**：**堂匣**。settle 完、下一手瞄准前，场上 0–1 只小木匣从天落下，落在可走土面（不落冰、深坑、虚空、熔浆）。场上最多 **2** 只，跨回合保留。直击 / 溅射 / 走进都拾：50% +1 飞步/影挪/霓弹/鼓息（还能持有的），35% +18 怒，15% **金匣** +28 怒。吐司「堂匣 · 术」/「堂匣 · 怒」/「金匣」。开局第一回合不刷。殿塌期间略勤（约 42% vs 28%）。`prefers-reduced-motion` 仍刷、无下落弹跳。AI：匣比敌人近且 HP≥40 才多走 36px，直击/残血击杀不追匣。不是第九把枪。十六图 / 叠珠 / 迟雷 / 风丝 / K 残影 / 断崖 / 沙脊 / 石门 / 霜泽 / 雷泽不改。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒。
> **v3.0 已上**：**时尽**。瞄准/走位倒计时沿用顶栏「时 N」，不另做第二只钟。弹堂 / 对坐 / 对堂 / 堂座 / 演习场 **18s**，堂核 **14s**，殿塌后 **11s**。末 5s 洋红脉动 + 可选秒 tick（静音无声）；`prefers-reduced-motion` 只改色不脉动。到 0 等同 X **过**，toast「时尽」，**无**半力白给。overlay 打开、弹在飞、迟雷引信未爆时停钟。AI 不吃这时钟。十六图 / 叠珠 / 迟雷 / 堂匣 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒。
> **v3.1 已上**：新图 **云台** `cloud`。两侧土岸出生 **168 / 772**，中央约 **220px** 石板在缝里左右滑（振幅约 **90px**，周期约 **7s**）。站在板上继承板 `vx`。坠入缝是坑（摔/埋，非虚空，除非掉出世界）。弹可挖板，坑半径 **×0.8**；板高过低则停成残石。对堂额外同岸、不站板。AI：敌在板上提前量 `x + vx*tof`，偏 **65°** 打板。本图雷泽约 **30%**。叠珠 / 迟雷 / 堂匣 / 时尽 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。标题十七图。禁止写传送 / 飞行 / 三叉戟 / 激怒。
> **v3.2 已上**：新图 **镜廊** `mirror`。两侧土岸出生 **168 / 772**，中央浅沟里一座约 **18px** 宽、**220px** 高的镜墙。侧击 / 擦墙 **vx × −1** 折一次（小火花，无吐司），下一碰才爆；顶砸（冲击 |nx| 小）立刻爆并啃石，坑半径 **×0.5**。剩高 **< 40%** 或实心柱太少则塌成残石，不再折。走进墙实心挡住；坠入沟是坑（摔/埋，非虚空）。对堂额外同岸。AI：敌在对岸时，折弹若更近则偏 **40–55°** 瞄墙，否则 **65°** 越墙。本图雷泽约 **30%**。叠珠 / 迟雷 / 堂匣 / 时尽 / 云台 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。标题十八图。禁止写传送 / 飞行 / 三叉戟 / 激怒。
> **v3.3 已上**：**余震**（TNT aftershock juice，不是新图、不是新枪）。坑半径 **≥ 36**，或 **高爆 / 三裂 / 迟雷** 爆，触发约 **0.28s** 一道余震：镜头抖 **3–5px**（`prefers-reduced-motion` 不抖）、坑沿再落 **4–7** 粒土屑（只画不伤）、90px 内薄沿（脚下土 **< 28px**）单位 **50%** 朝坑滑 **8–14px**（可埋，不是瞬移）。场上只一道，第二发刷新时长。无吐司。直击顿帧仍 **140ms**，余震是爆后加料。十八图 / 八枪 / 风丝 / K 残影 / 堂匣 / 时尽不改。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒。
> **v3.4 已上**：新图 **井口** `well`。两侧土岸出生 **168 / 772**，中央一口约 **240px** 宽的深井，底有暗水盘（比月池窄、更深）。**90°** 从岸唇砸进井，**30°** 可越唇打对岸，**65°** 常削近唇。井内走 **0.40×**，回合开始浸 **4**。爬出靠炮弹砸出坡，或 **飞步** 越唇。石唇坑 **×0.75**，底泥坑 **×1.15**。对堂额外同岸、不进井。AI：敌在井内偏 **90° / 高爆**，近出口 **迟雷** 封爬；对岸偏 **65°**。本图雷泽约 **30%**。大坑仍吃 **余震**。叠珠 / 迟雷 / 堂匣 / 时尽 / 云台 / 镜廊 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。标题十九图。禁止写传送 / 飞行 / 三叉戟 / 激怒。
> **v3.6 已上**：**堂袋**。画布下 `#item-dock` 7 格木石托盘（经典底栏，原创名）。巽符 / 沉符 / 重石 / 轻靴 / 护符 / 回春 / 锐角。每人独立袋，开局 2（演习场 3）。点按叠加武装，金环抬起；回春点按立刻 +14。除回春外开火消耗。堂匣非金时 35% 改发袋物。飞步等术仍在顶栏 `.items`。键 `- = [ ] \ ; '`。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒。  
> **v3.7 已上**：**堂袋 polish**。7 枚 SVG 改 40 viewBox 宝石/石框卡（斜面+内高光+投影），36px 可读；格 56×72，珐琅角标；武装金斜面 + CSS 火花（减动效关火花）；武装芯片「巽+沉+重」；空格 0.12s 晃；堂匣发袋物吐司「堂匣 · 巽符」跟图标同色；瞄准力旁「袋」。不加物、不改图、`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒。
> **v3.8 已上**：堂袋再两枚。**潮符** `tide`（键 `,`：本发坑 +4px，溅射打到薄沿再朝坑推 10px）/ **霁符** `clear`（键 `.`：本发风加速 ×0.5）。开局 2、演习场 3，叠武装、开火消耗同巽/沉/重。芯片含 潮 / 霁；堂匣可抽。狠：|风|≥6 远射霁，敌在薄沿潮；易不用。托盘桌面仍一行，仅宽不足 700px 折两行。v3.7 宝石卡/火花/袋标/同色吐司保留。十九图 / 八枪 / `GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.0 已上**：**堂袋重做**。底栏 `#item-dock` 换成经典 ×2 / ×3 / +1 / +2 / +3 / +5 / 回春。×2 与 ×3 互斥；+N 可叠加算。每壳因子 `(x2?0.90:x3?0.60:1)*plusMul`。开火扣体，体不够不能打。叠珠仍是第 7 把枪。术仍在顶栏。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.1 已上**：**堂袋 glyph polish + use juice**。7 枚厚描边宝石卡（×2 金 / ×3 绯红 / +1 奶油 / +2 橙 / +3 猩红 / +5 金箔 / 回春叶）。芯片「×2++3++2」跟图标同色。开火叠物：炮口混色喷（减动效关）+ 格闪 0.18s。不改叠算 / 体 / 互斥 / 图 / 枪。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.2 已上**：**堂袋 stack readout**。瞄准 / 蓄力且有武装时，力旁紧凑标 `2发×1.35`（发数 × shellMul）。仅 +N（无 ×2/×3）时 `+50%`（等同 ×1.50）。体不够标变红，吐司「体不够」仍在。芯片「×2++3」保留。不改叠算 / 体 / 图 / 枪。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.3 已上**：**堂匣 drops ×/+ more, AI combos**。堂匣非金袋物 **50%**；抽袋偏 ×2 / +3 / +2 略高于 回春；吐司「堂匣 · ×2」跟 `BAG_TINT`。狠：敌暴露且体≥60 叠 ×2++3（无 +3 则 +2）；近距且体≥80 偏 ×3++2；永不叠 ×2 与 ×3。中：只 ×2 无 plus。易几乎不用。不改叠算 / 体 / 图 / 枪。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.4 已上**：新图 **洞顶** `cave`。两侧土岸出生 **168 / 772**，中央浅坑 + 洞顶钟乳（牙尖约 y 210）。**90°** 砸顶，**65°** 削近牙，**30°** 越坑打对岸。牙下可走。洞顶坑 **×0.70**。对堂额外同岸。AI：敌在牙下偏 **90° / 高爆**；对岸 **30°**；岸唇 **迟雷**。本图雷泽约 **20%**。堂袋 / 堂匣 50% / 八枪不改。`GRAV` 260、`VK` 420。标题二十图。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.5 已上**：**落顶**（TNT cave-fall 系统，原创名）。洞顶 / 牙 `carveCave` 返回 true 时从击点落 **2–4** 粒石屑（外形沿用 `rainCaveCrumbs`）。短命抛体：重力 `GRAV` 260、无风、半径约 **4px**。出生 **0.55s** 内碰活人 **3** HP（堂核 ×1.15），小 punch，不埋、不吐司（HP 到 0 才走击破）。同一爆破每人一粒。碰土面 / 地板消失。`prefers-reduced-motion` **不刷**伤害石屑。只 **洞顶**。AI 不瞄准石屑。大坑仍吃 **余震**。堂袋 ×2×3 +N / HUD `2发×1.35` / 堂匣 50% / 八枪 / 二十图不改。overlay `pointer-events: none`。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.6 已上**：新图 **齿岸** `teeth`（TNT sawtooth-hills 系统，原创名）。两侧土岸出生 **168 / 772**（顶 y≈308），中间四座石牙：峰约 x **300 / 420 / 540 / 660**，峰 y≈**210**，谷 y≈**360**，牙宽约 **70px**。谷里是土（可走 **0.90×**），峰是石（坑 **×0.75**，满速）。**30°** 可越两牙打对岸；**65°** 常削近牙；**90°** 砸近谷可埋。坠入谷是坑（摔/埋），非虚空。对堂额外同岸，开局不站峰。AI：敌在谷内偏 **90° / 高爆**；对岸 **30°** 越牙；岸唇 **迟雷**。本图雷泽约 **30%**。落顶仍只洞顶。堂袋 ×2×3 +N / HUD `2发×1.35` / 堂匣 50% / 八枪 / 洞顶 / 井口不改。overlay `pointer-events: none`。`GRAV` 260、`VK` 420。标题二十一图。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.7 已上**：**袋火**（TNT Dual/Triple trail 系统，原创名，不是第九把枪）。×2 / ×3 开火时，该齐射 **每一壳**（含后续 0.32s）画短拖尾：×2 金 `#ffe36b`、×3 绯红 `#dc143c`（`BAG_TINT`）。叠 +N 则屑混 plus 色。后续发略亮。炮口混色喷保留。`prefers-reduced-motion` **跳过拖尾**，炮口喷仍跳过。不改叠算 / 体 / 图 / 枪 / 落顶。HUD `2发×1.35` / 堂匣 50% / 二十一图 / 八枪不改。overlay `pointer-events: none`。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.8 已上**：×3 改同时分裂。一发三壳同膛出门，角 ±8°（夹 0–180），每壳 ×0.60，不再 0.32s 连打。×2 仍是后发。叠珠+×3 只裂本发三壳，不排队第二发。名字仍 ×3，禁止写三叉戟。体不够卸武装，普通弹照打（走路仍耗体，吐司「体不够」仍在）。袋火 / HUD `3发×0.60` / 二十一图 / 八枪 / 落顶不改。overlay `pointer-events: none`。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.9 已上**：**金匣袋**（TNT gold-box Dual drop，原创名，不是第九把枪）。金匣仍每次 +28 怒。另 **40%** 再 +1 堂袋（偏 ×2 / +5 / +3，高于 回春）。双掉两短吐司：先「金匣」，再「金匣 · ×2」跟 `BAG_TINT`。`maybeBagCrate('gold')` 仍返回 `'gold'`。非金堂匣袋 50% 不改。叠算 / 体 / ×3 同时分裂 / 袋火 / 落顶 / 八枪 / 二十一图不改。overlay `pointer-events: none`。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.10 已上**：新图 **环坑** `ring`（TNT crater-bowl 系统，原创名。干坑，不是月池水、不是井口）。两侧土岸出生 **168 / 772**（顶 y≈308）。中央圆干坑：沿约 x **480**、沿 y≈**250**、底 y≈**390**、内径约 **140px**、石沿带宽约 **36px**（坑 **×0.72**）。坑底是土，走 **0.55×**。坠入是坑（摔/埋），非虚空、非水，**无**回合浸伤。**30°** 可越坑打对岸；**65°** 常削近沿；**90°** 砸坑可埋。对堂额外同岸，开局不站坑、不站沿。AI：敌在坑内偏 **90° / 高爆**；对岸 **30°** 越坑；近沿 **迟雷**。本图雷泽约 **25%**。金匣袋 / ×3 同时分裂 / 0 体开火 / 袋火 / 落顶 / 齿岸 / 洞顶 / 井口 / 月池 / 堂袋 / 八枪不改。overlay `pointer-events: none`。`GRAV` 260、`VK` 420。标题二十二图。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.11 已上**：**蓄缓**。蓄力 **2s** 走满 100（原 0.8s）。点射仍 12。堂袋 +N 仍可叠，不削互斥。overlay `pointer-events: none`。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。
> **v4.12 已上**：**蓄条**（TNT charge-meter juice，原创名，不是第九把枪）。蓄力时 HUD「力 N」旁短模拟条从 `TAP_POW` 填到 100，数字仍更新。到 100 条与力标签金闪 **一次**，仍可松。标题 overlay 隐藏。`pointer-events: none`。`prefers-reduced-motion`：条照填，不闪。不改 `CHARGE_T` 2、物理、堂袋叠算、二十二图、八枪。overlay `pointer-events: none`。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

---

## 1. 概述与定位

### 1.1 世界观

云海之上有一座会呼吸的石殿，殿名 **弹堂**。殿息化为风。弹师以灵核凝弹，在可被轰碎的石面上走位、拉角、对轰。赢的人把对方从殿上轰下去，输的人落进云里。

- 玩家：**岚丸**（青焰狸灵，左台，炮口默认 65°）
- 对局 AI：**烬丸**（赤砂狸灵，右台，炮口默认 115°）
- 2v2 同伴（v1.2 对堂）：岚丸侧 **霜丸**（玩家），烬丸侧 **霆丸**（AI）。玩家操作左队两人。  
- 2v2 热座（v1.6 堂座）：左队岚丸+霜丸、右队烬丸+霆丸皆玩家。无 AI。对堂 ≠ 堂座。

无第三方角色，无现成商店素材。视觉：深色霓虹，青 `#00e8ff` / 洋红 `#ff3db8` / 金 `#ffe36b` / 紫 `#8b5cff`，跟游廊一家。中文 UI，英文短副标题 `TAN HALL`。

### 1.2 一句话

看风，拉满或点射，把对面从石殿上轰下去。

### 1.3 幻想（fantasy）

玩家是弹师，不是枪兵。每一发都是「读风 + 选学校 + 赌落点」。地形是第三名玩家：你挖它，它让人摔死。怒气攒满后下一发叫 **殿破**，把石面连人一起掀开。道具不掉地上，是弹师随身的殿术，每局限次。

30 秒内能懂。标题页一句话 + 底栏操作，无长教程。演习场（v0.2）是唯一「教瞄准」的地方，用格尺和 65° 表，不弹对话框。

### 1.4 我们偷的是系统，不是 IP

从「回合制 2D 弹道 + 可毁地形」这个类型里只借 **规则骨架**，名字、词、角色、地图全部原创。

| 借来的系统（可以做） | 绝不能抄的 |
|---|---|
| 走位有上限，一回合一发 | 角色名、武器名、道具名、地图名 |
| 重力 + 水平风偏 | UI 词：传送 / 飞行 / 三叉戟 / 激怒 |
| 角度学校：15 / 30 / 45 / 65 / 90 | 任何「弹弹×」商标或关卡名 |
| 挖坑、脚下掏空、摔伤、坠出底边 | 别人的商店立绘、音效包、字体品牌 |
| 限次道具、怒气必杀、2v2 延迟条 | 别人的技能图标与连招名 |

产品名永远写 **弹堂**，英文 **TAN HALL**。目录 `g/tan-tang/`。localStorage 前缀 `playbox-tan-tang-`。

### 1.5 平台与体量

单页 Canvas 游戏。桌面键盘为主，触屏有虚拟键。无账号、无联网、无 CDN、无暂停、无悔弹。一局 2–8 分钟。

---

## 2. 已上架的 v0.1

下面是 **2026-08 当前 build 的诚实清单**。没写的东西就是没有。不要在补丁里假装它们已经存在。

### 2.1 有什么

| 项 | 现状 |
|---|---|
| 模式 | **弹堂**（`kind='hall'`，HP 100，风 −8～+8）、**堂核**（`kind='core'`，HP 60，风 −14～+14，伤害 ×1.15） |
| 地图 | **平原** `plain`、**峡谷** `canyon`、**双台** `twin`。标题页点选，默认平原 |
| 角色 | 岚丸（玩家，左）vs 烬丸（AI，右）。1v1 only |
| 武器 | **普通弹 / 高爆 / 穿透**，开局三把都有，无限弹药，每回合一发 |
| 走位 | 本回合 **96px** 贴地横移，走完即停，**不能跳** |
| 瞄准 | 0°～180°，↑↓ / WS 连调，约 70°/s；空格 / Z 按住蓄力 2s 走满 100，点射 12 |
| 风 | 每回合重掷，只改水平加速度，弹在空中时不变。顶栏箭头+数字 |
| 地形 | 960 列高度图，圆形挖坑即时写回。脚下空则坠落。掉出画布底 = 坠亡（HP=0） |
| 超时 | 20s 未发射 → 强制 **力 50、当前角度** 打出。toast「超时 · 半力打出」 |
| 跳过 | **无**。不能空过回合 |
| 道具 | **无** |
| 体力条 | **无**。HUD 只写「步 96」，与走位预算是同一件事 |
| 怒气 / 殿破 | **无** |
| 格尺 / 小地图 / 上回残影 | **无** |
| 演习场 | **无** |
| 2v2 | **无** |
| 暴击 | **无** |
| 托管 / 自动 | **无**。AI 只在烬丸回合自己打 |

### 2.2 文件（只许这些，v0.1）

```
g/tan-tang/
  index.html
  css/style.css
  js/game.js
  preview.png          封面，铺在标题天空后
  art/hero.png         岚丸精灵
  art/foe.png          烬丸精灵
  DESIGN.md
  PLAYTEST.md
  GDD.md               本文
```

无 CDN、无库、无联网。`g/tan-tang/index.html` 直接打开能玩。

### 2.3 已写死的常量（v0.1，改手感先改这里并同步本文）

| 符号 | 值 | 含义 |
|---|---|---|
| `VW` `VH` | 960 × 540 | 虚拟场 |
| `STEP` | 1/60 | 逻辑步 |
| `UNIT_R` | 14 | 身体半径 px |
| `WALK_PX` | 96 | 每回合步行预算 |
| `CHARGE_T` | 2 | 蓄力满档秒 |
| `TAP_POW` | 12 | 点射力度 |
| `TURN_T` | 20 | 回合超时秒（v3.0 改为 18，见 §3.4 时尽） |
| `GRAV` | 260 | 重力 px/s²（向下为正） |
| `VK` | 420 | 力度 100、无 loft 时的基准初速 |
| `LOFT_W` | 20 | loft 高斯宽度（度） |
| `LOFT_B` | 0.55 | loft 峰值加成 |
| `WIND_K` | 2.05 | 风 → 水平加速度系数 |
| `BEST_KEY` | `playbox-tan-tang-best` | 连胜 / 最快 / 上次模式地图 |
| `MUTE_KEY` | `playbox-tan-tang-mute` | `"1"` 静音 |

Loft 公式（已上架，65° 最远的物理来源）：

```
elev(ang) = ang<=90 ? ang : 180-ang
loft = 1 + 0.55 * exp( -((elev-65)^2) / (2 * 20^2) )
speed = 420 * (clamp(power,1,100)/100) * wep.spd * loft
```

65° 时 loft=1.55，初速 = 6.51 × 力度（普通弹）。45° 时 loft≈1.334。90° 时 loft≈1.252，但竖直分量几乎吃掉全部水平距离。

### 2.4 开局坐标

| 图 | 岚丸 x | 烬丸 x | 地面特征 |
|---|---|---|---|
| 平原 | 152 | 768 | 缓丘，高度约 360–430 |
| 峡谷 | 122 | 838 | 中央深 V，两侧垫平 y≈306 |
| 双台 | 148 | 812 | 左右高台，中央断层接近底边 |

角色 y = `groundAt(x) - 14`。炮口在身体前 18px、上偏 4px。

### 2.5 胜负文案（已上架，不要改词）

| 结果 | 标题 | 副文 |
|---|---|---|
| 烬丸 HP≤0 | 堂破了 | 烬丸倒了。N 回合 · 连胜 X · 最快 Y 回 |
| 岚丸 HP≤0 | 落堂了 | 岚丸倒下。本局 N 回合 |
| 同时清零 | 对坠 | 同烬。本局 N 回合 · 连胜清零 |

结束层按钮：「再来」（同模式同图）「换一局」（回标题）。R 随时重开。标题页 R / 空格 / Enter / 1 开弹堂，2 开堂核。

### 2.6 明确不做（v0.1 限制，v0.2 只解开本文写明的）

- 不移动射击、不即时制、不第三人、不地上掉落物
- 无暂停键、无悔弹、无手柄
- 不抄任何外部 IP 名

---

## 3. 核心循环

### 3.1 状态机

```
title ──开始──► play
                  ├ aim      走位 / 调角 / 切武器 / 用道具
                  ├ charge   蓄力中（仍可调角，不可走；v0.2 道具见 §8）
                  ├ fly      弹在飞
                  ├ settle   挖坑后等人落地，最少 0.18s
                  └ end      叠在 overlay，可再来 / 换一局
play ──HP 判死──► end ──换一局──► title
任意处 R ──► 同模式同图重开（标题页 R = 开一局弹堂）
```

`G.mode`：`title` | `play` | `end`  
`G.phase`：`aim` | `charge` | `fly` | `settle` | `end`  
`G.turn`：`p` | `f`（2v2 改为单位 id，见 §6.5）

### 3.2 一回合逐步（1v1）

1. `beginTurn(who)`  
   - `phase='aim'`，`charging=false`，`power=12`，步行预算拉回 96（v0.2：体力回 100，步行预算仍 96）  
   - `timeout=turnTime()`（弹堂 18 / 堂核 14 / 殿塌 11）  
   - `rollWind()`，顶栏立刻改箭头和数字  
   - 岚丸回合：`turns += 1`（回合数只在玩家出手时 +1）  
   - toast：「岚丸的回合 · 风 …」或「烬丸瞄准中 · 风 …」  
   - 烬丸回合：立刻 `startAI()`（先算后演，见 §12）
2. 行动方左右走（贴地，消耗步行 / 体力），↑↓ 调角，1/2/3 切武器。v0.2 可在 aim 用道具。
3. 空格 / Z / 触屏「蓄」/ 画布按住 → `charge`，力度从 12 线性拉到 100，时长 2s。满档金闪，仍可松。
4. 松手 → `fire` → `phase='fly'`。弹从炮口出膛，带当前风。
5. 弹碰人 / 碰地 / 出界 → `explode`：挖坑、结算直击/溅射、粒子、音效。打中才顿帧。
6. `phase='settle'`，`settleT=0.18`。双方做重力落地。落差≥36px 摔伤。掉出 `y > 540+40` 坠亡。
7. `unitsSettled()`（双方落地且 `|vy|<4`，或已死）且 settle 倒完 → `checkEnd()`。没结束则换手。

弹在空中与 settle 期间：**不能走、不能调角、不能切武器、不能用道具、时尽钟停**（只在人类 aim/charge 扣；overlay / 迟雷引信也停）。

### 3.3 谁先动手

| 模式 | 先手 |
|---|---|
| 弹堂 / 堂核 1v1 | **永远岚丸**。`startGame` 后 `beginTurn('p')` |
| 演习场 v0.2 | 只有岚丸。没有烬丸行动。木桩不还手 |
| 2v2 v1.0 | 延迟最低者。开局岚丸 delay=0，见 §6.5 |

没有「猜拳」「掷先手」。刷新、重开、换图都不改先手。

### 3.4 时尽（v3.0）

- 名字 **时尽**。瞄准/走位可见倒计时，沿用顶栏「时 N」，不另做第二只钟。
- 时长：弹堂 / 对坐 / 对堂 / 堂座 / 演习场 **18s**；堂核 **14s**；殿塌后 **11s**（盖过堂核）。
- 只对 **当前人类** 的 aim/charge 倒数。烬丸 / 霆丸 AI **不吃** 这时钟，仍用自己的思考延迟。
- 末 5 秒：顶栏变洋红并脉动；每秒短 tick。静音仍无声。`prefers-reduced-motion`：只改色，不脉动。
- 到 0：等同 X **过**（不发射、不挖坑）。toast「时尽」。殿破若已点亮则浪费。2v2 延迟按跳过 +80。
- 暂停：overlay 打开、弹在飞、场上 **迟雷** 引信未爆。飞步 / 影挪点地期间钟不停。
- 禁止半力打出。禁止第二只钟。

### 3.5 跳过（v0.2 新增，v0.1 不要做）

- 键：**X**。顶栏工具区加按钮「过」。触屏道具条最右一颗「过」。
- 仅 `phase==='aim'` 且轮到自己。charge 中不能过（先松手取消蓄力：松开但 power 仍 12 且未进入 fly —— 见下条）。
- 取消耗：不发射、不挖坑、怒气不变、殿破若已点亮则 **浪费**（怒气已扣，标记清掉）。
- 立即 `phase='settle'`，`settleT=0.10`，然后换手。
- **取消蓄力**：charge 时按 ESC 或再点「蓄」以外的空白不取消（避免误触）。取消方式：按 X 之前必须先松开空格；松开空格时若 `power<=13` 且按住时长 < 0.08s，视为点射（保持 v0.1：未蓄松手 = 力 12 打出）。要跳过必须在 aim 按 X，不要在 charge 松手。
- 被 **霓弹冻结** 的自动跳过：见 §8.4，不是玩家按 X。

### 3.6 风的重掷

```
m = 弹堂? 8 : 14
若 堂核 且 rand<0.55：w = ± irand(8, m)     // 更易大风
否则：w = irand(-m, m)
若 w==0 且 rand<0.35：w = ±1               // 少出现绝对无风
G.wind = w
```

符号：**正 = 向右吹（→）**，负 = 向左吹（←）。顶栏：`风 → 3` / `风 ← 3` / `风 · 0`。  
弹在空中风锁死。settle、走位、道具位移都不吃风（飞步的跳跃弧是固定本地抛物，不吃本回合风，见 §8.2）。

### 3.7 连堂

连续两回合（跨换手，按「造成有效伤害的射击」计）打出直击或有效溅射：`combo += 1`，≥2 时蹦「连堂 ×N」，音阶上行。打空（只挖地、0 伤害）清零。摔伤、坠亡、道具位移 **不加** 连堂。v0.2 霓弹命中（冻住）算有效，combo+1。跳过清零。

---

## 4. 物理与瞄准

距离单位全游戏统一：

| 单位 | 定义 |
|---|---|
| 1 格 | **48 px** |
| 满屏宽 | 960 px = **20 格** |
| 满屏高 | 540 px = 11.25 格 |
| 身体 | 半径 14 px ≈ 0.29 格 |
| v0.1 步行 | 96 px = **2 格** |

HUD、格尺、演习场表、本文所有「学校」都用格。代码里继续用 px，转换 `格 = px/48`，四舍五入到 0.1 格给 UI。

### 4.1 坐标系与积分

- 原点左上。x 向右，y **向下**。
- 角度：0° 朝右，90° 朝天，180° 朝左。与数学极角一致（炮口 `cos(θ), -sin(θ)`）。
- 每逻辑帧 dt=1/60：

```
vx += wind * 2.05 * dt
vy += 260 * dt
x  += vx * dt
y  += vy * dt
```

- 出膛点：`(unit.x + cos(θ)*18, unit.y - 4 - sin(θ)*18)`
- 弹半径视觉：普通/穿透 3.6，高爆 5.2。判定：弹心碰到身体半径+5（即中心距 ≤ 19）= 直击。
- 出界：`x<2` 或 `x>958` 或 `y>560` → 在夹紧后的点爆炸（仍挖坑，可能打空）。
- 模拟上限约 420 步（7s），超时当空爆。

穿透见 §7。人物每帧贴地：`grounded` 时 y = groundAt(x)−14；脚下地面比人低 2.5px 则进入坠落，`vy` 从 0 加重力。

### 4.2 为什么 65° 最远

同一力度下，loft 在 elev=65° 取峰 1.55，且 `sin(2×65°)=0.766` 接近 45° 的 1.0，两者相乘后 **65° 水平距离最大**。这是已上架手感，不要改 loft 峰到 45°。

平原无风、普通弹、力 70、角 65：水平落点相对炮口约 **520–680 px**（selfCheck 窗口 500–700，对岸开局约 616 px）。  
同力 45° 比 65° 近至少 8 px，且更平、更快。  
同力 90° 落点距炮口 **< 50 px**，风只左右偏一点点。

炮口虚线 **只画约 80 px 的初速方向**，不含风、不含重力。不要替玩家画整条预判线（演习场例外，见 §11.3）。

### 4.3 五种学校（给玩家和 AI 的口诀，不是改物理）

物理永远用 §4.1。学校是 **查表口诀**，写在演习场和底栏，AI 也可以先用学校出初值再枚举精修。

| 学校 | 仰角（朝右） | 朝左时的角 | 用途 |
|---|---|---|---|
| 15° | 15 | 165 | 贴地穿缝、近身砸脚、双台台沿 |
| 30° | 30 | 150 | 中近、低风、要快弹 |
| 45° | 45 | 135 | 平快略近，堂核抢拍 |
| 65° | 65 | 115 | **默认远射**。力决定落点 |
| 90° | 90−格 | 90+格 | 近中距精细打点，力锁 95 |

岚丸开局 65，烬丸开局 115。换手不重置角度（v0.1 已如此：单位自己记得 `ang`）。

### 4.4 65° 力度表（同高、无风、普通弹）

由当前物理 `R ≈ 0.1249 · power²` px 反推，`power = 19.61 · √格`，四舍五入。  
**这张表必须画在演习场右侧**，对局中格尺高亮当前格对应行。

| 格 | 水平 px | 力 | 格 | 水平 px | 力 |
|---:|---:|---:|---:|---:|---:|
| 1 | 48 | 20 | 11 | 528 | 65 |
| 2 | 96 | 28 | 12 | 576 | 68 |
| 3 | 144 | 34 | 13 | 624 | 71 |
| 4 | 192 | 39 | 14 | 672 | 73 |
| 5 | 240 | 44 | 15 | 720 | 76 |
| 6 | 288 | 48 | 16 | 768 | 78 |
| 7 | 336 | 52 | 17 | 816 | 81 |
| 8 | 384 | 55 | 18 | 864 | 83 |
| 9 | 432 | 59 | 19 | 912 | 85 |
| 10 | 480 | 62 | 20 | 960 | 88 |

力小于 12 的格不要用 65°（点射都是 12，约 0.3 格）。满屏 20 格力 88，留 12 点给逆风/高台。  
高爆：表上力 × 1/0.88 ≈ **×1.14**（它初速慢）。  
穿透：表上力 × 1/1.06 ≈ **×0.94**。  
堂核不改表（伤害变、弹道不变）。

查表步骤（65° 学校，朝右）：

1. `dx = 目标x - 自己x`（朝左则取反）。`格 = round(dx / 48)`，夹到 1–20。
2. `力 = 表[格]`
3. **风**：顺 +、逆 −。  
   `elev = 65 + 2 * 风_along`  
   `风_along = wind * facing`（facing +1 朝右，−1 朝左）  
   例：风 →3、岚丸朝右：elev=65+6=71°。  
   例：风 →3、烬丸朝左：风_along=−3，elev=65−6=59°，实际角=180−59=121°。  
   角夹在 5–175。
4. **高度补偿**（§4.6）改力，不改这步的角。
5. 微调：每偏 0.5 格，力 ±2。

### 4.5 90° 学校

力 **锁 95**。只调角。

```
格 = max(0, min(20, round(水平距离 / 48)))
elev = 90 - 格 + 2 * 风_along
```

- 朝右：`ang = elev`
- 朝左：`ang = 180 - elev`
- 夹紧 5–175。格=0 时就是 90° 砸脚边。
- 例：距 5 格、无风、朝右 → 85° / 95。  
  距 5 格、风 →3、朝右 → 85+6=91° / 95。  
  距 8 格、风 ←4、朝右 → 82−8=74° / 95。

90° 学校吃高度补偿改的是 **角**，不是力（力仍 95）。

### 4.6 高度补偿

画布 y 向下。对方站得更高 ⇒ `groundAt(对方) < groundAt(自己)`。

```
Δh_px = groundAt(自己.x) - groundAt(对方.x)     // 正 = 对方在高处
Δh_格 = Δh_px / 48
```

| 学校 | 补偿 |
|---|---|
| 15 / 30 / 45 / 65 | `力 += round(3 * Δh_格)`，再夹 12–100 |
| 90 | `elev -= round(Δh_格)`（高处目标更平一点），力仍 95 |

对方在低处（坑里）Δh 为负：65° 减力，90° 把 elev 加回去（更接近 90，更短）。  
补偿只改口诀，物理引擎不单独加项。演习场要画「同高力 / 补偿后力」两行。

### 4.7 15° / 30° / 45° 速查（同高无风，普通弹）

给演习场第二页用。不必做成对局常驻表。

**45°**：同力比 65° 近约 4–8%。口诀：先查 65° 表，力 **+4**，或格数按 ×1.05 再查。  
**30°**：力约 65° 表 ×1.22。近距 1–6 格好用。  
**15°**：力约 65° 表 ×1.55，且落地角很平，容易擦地提前爆。只打 ≤4 格或直线掏脚。

### 4.8 风对弧线的肉眼标准

同一发 65° / 70，风 −8 与 +8 的落点 x 差必须 **> 24 px**（selfCheck 已要求左右各偏 12+）。拖尾 28 点，风非零时弧线肉眼能看出被吹弯。顶栏风标同时在。v2.2 天空再加 **风丝**（条数 / 速度随 `|风|`，方向跟号；`|风|≥5` 更粗更快），近地可有叶尘。`prefers-reduced-motion` 藏丝留顶栏数字。风丝不进物理。不要做「暗风」。

### 4.9 伤害与挖坑（已上架，v0.2 不改公式）

直击：弹心距身体中心 ≤ `r+5`（19px）→ `wep.direct * dmgMul`。  
溅射：距爆心 `< wep.splash` 时

```
spl = wep.direct * 0.72 * (1 - d/splash)^1.1 * dmgMul
```

直击与溅射 **取高，不叠满**。`dmgMul` 堂核 1.15，弹堂 1.00。殿破再 ×1.6（§9）。  
`hurt`：至少 1，四舍五入，扣到 0。自杀、误伤、摔伤、坠底都算。

摔伤（本段坠落累计 `fall`，只在落地时结算）：

```
若 fall >= 36：
  raw = min(28, floor((fall - 36) * 0.35))
  dmg = max(1, round(raw * dmgMul))
```

坠亡：`y > 540+40`，HP 直接 0，飘字「坠亡」。不走摔伤公式。

挖坑：对每列 x，若 `|x-cx| < r`，把高度图向下推到圆的下沿（y 更大 = 地面更低）。穿透额外沿速度方向再掏一条短隧道（§7）。

人站坑沿：下一帧 `ungroundIfAir`，滑落贴新地面。可以连环挖，把自己或对方埋进坑再砸。

---

## 5. 操作与 HUD

### 5.1 键盘（v0.1 已有 + v0.2 新增）

方向键与 WASD 等价。方向键、空格 `preventDefault`。`touch-action: none`。

| 键 | 阶段 | 作用 |
|---|---|---|
| ← → / A D | aim | 左右走。贴地。耗步行 / 体力。走完即停 |
| ↑ ↓ / W S | aim、charge | 调角，70°/s，0–180 |
| 空格 / Z | aim→charge→松手 fly | 按住蓄力，松开发射 |
| 1 / 2 / 3 / 4 / 5 / 6 / 7 / 8 | aim、charge | 普通弹 / 高爆 / 穿透 / 三裂 / 霓轨 / 霓火 / 叠珠（v2.4） / 迟雷（v2.7） |
| R | 任意 | 重开。标题页开弹堂；对局/结束层同模式同图 |
| M | 任意 | 静音，写 `playbox-tan-tang-mute` |
| Enter / 空格 / 1 | title | 开弹堂 |
| 2 | title | 开堂核 |
| 3 | title v0.2 | 开演习场 |
| **4** | title v0.6 | 开对坐（对局里 4 仍是三裂） |
| **5** | title v0.9 | 开对堂（对局里 5 仍是霓轨） |
| **6** | title v1.6 | 开堂座（对局里 6 仍是霓火） |
| [ / ] | title | 切地图 |
| **Q** | aim v0.2 | 飞步 |
| **E** | aim v0.2 | 影挪（进入点地态） |
| **C** | aim v0.2 | 装备霓弹（本发改为霓弹；再按 C 卸下） |
| **V** | aim v0.2 | 鼓息 |
| **B** | aim | 逆息 |
| **G** | aim v1.4 | 障幕 |
| **F** | aim v0.2 | 殿破（怒气 100） |
| **X** | aim v0.2 | 跳过 |
| **Tab** | aim/charge v0.2 | 开关格尺（默认开） |
| **K** | 任意 v2.0 | 开关上回残影（默认开，`best.ghost`） |
| ESC | 影挪点地态 | 取消影挪，回 aim |

无手柄。无暂停。无悔弹。

### 5.2 触屏 / 指针

画布下虚拟键（已有）：**左 / 右 / 仰 / 俯 / 蓄**。按住蓄、松开发射。  
v0.2 在虚拟键 **上方** 加一条道具条：飞步 / 影挪 / 霓弹 / 鼓息 / 逆息 / 障幕 / 过。殿破在怒气条满时亮在血条旁，可点。

鼠标 / 手指点画布：

- 默认：拉炮口朝向指针，同时开始蓄力；松开发射（v0.1 已有）。
- 影挪点地态：第一次按下是选落点，不蓄力。第二次不需要——按下即确认（见 §8.3）。
- 演习场：点木桩不造成「对自己蓄力」以外的事；仍是拉角蓄力。

### 5.3 HUD 常显（顶栏，已有 → v0.2 加粗）

已有：连胜、最快 N 回、模式名、地图名、烬丸档「烬 · 中」、风、角、力、步、连堂、双方 HP 条与数字、武器三钮、静音、重开。

v0.2 改 / 加：

| 槽 | 内容 | 规则 |
|---|---|---|
| 步 → **体** | `体 100` 递减 | 与体力同步。步行仍最多 96 |
| 怒 | `怒 0`～`100`，条宽 72px，青到金 | ≥100 闪金，可点「殿破」 |
| 道具次数 | 四钮角标：飞 `2` 影 `1` 霓 `2` 鼓 `1` | 用完灰掉 |
| 上回残影 | 角/力旁淡金字 `上 65°/70` | 上一发（任何人）落地后的下一手瞄准期。开局第一回合不显示。K 关则隐 |
| 雷泽 | 芯片 `雷泽` | 本局天气开启时显示。不改地图名 |
| **时** | `时 18` 倒数 | 只人类瞄准/蓄力。堂核 14，殿塌 11。末 5s `.pulse`。AI 回合隐藏 |
| 格尺 | 画布内，见 §5.5 | Tab 关。演习场强制开 |
| 小地图 | **v0.3** 才做，v0.2 不要做 |

血条数字用 `Math.ceil`，0 才空。堂核模式名加 `.hot`。

### 5.4 上回残影（v2.0）

任意一发落地（`explode`）记下本发路径折线。只活在本局内存，不写盘面到 localStorage。

```
ghost = { x, y, ang, power, wepId, wind, points[] }
```

`points` 是本发从炮口到落点的全长折线（与拖尾 42 点分开记，不截断）。

下一单位 phase=aim/charge（**所有人**：岚丸 / 烬丸 / 霜丸 / 霆丸 / 对坐换手）：

1. HUD 淡金小字 `上 {ang}°/{power}`，例 `上 65°/70`。
2. 画布用 **金色 30% 透明虚线** 重描 `points`，不随本回合风改。这是「上一发打到哪」，不是预判。
3. **弹在飞时不画**（那是本发拖尾）。settle / 结束层 / 标题也不画。
4. 换武器不擦。跳过 / 冻结不刷新，下一手仍看上一发实弹。
5. 重开、换图、回标题清空。第一回合无残影。
6. 键 **K** 开关，默认开。偏好写入 `best.ghost`（布尔，缺省 true）。路径本身不存。

### 5.4b 风丝（v2.2）

天空横向 **风丝**，只画不改物理（`GRAV` 260、`VK` 420、`WIND_K` 2.05 不动）。

- 条数、速度随 `|风|` 变；方向跟风号（正 → 右，负 ← 左）。无风不画。
- `|风|≥5`：**更粗、更快**。顶栏箭头变双（`→→` / `←←`），数字仍立刻更新。
- 近地可选极小叶 / 尘，贴地飘，不挡操作。
- `prefers-reduced-motion`：藏丝与叶尘，**顶栏数字仍在**。
- 画在画布里，无第二层 DOM。overlay 仍 `pointer-events: none`。
- 断崖、上回残影 K 不改。v2.3 起十四图（含沙脊），风丝规则不变。

### 5.4c 雷泽（v2.5）

天气系统。名字 **雷泽**。不是传送、不是飞行，不改图名。

- **风谷 / 断崖 / 沙脊**：开局必开。
- **熔台**：永不开。
- **云台 / 镜廊 / 井口 / 齿岸**：开局约 **30%**。
- **洞顶**：开局约 **20%**。
- **环坑**：开局约 **25%**。
- 其余图：开局约 **35%**。
- 视觉：天空略暗；画布闪雷 **80–160ms**；雨丝。无第二层 DOM。`prefers-reduced-motion`：**藏闪与雨**，HUD「雷泽」仍在。
- 闪电节拍：对局时间每 **8–14s** 一次。overlay 打开时不跳、不计这段时间。
- 风：该节拍风可跳 **±1**，仍夹 `windMax()`（弹堂 ≤8，堂核 ≤14）。`GRAV` 260、`VK` 420、`WIND_K` 2.05 不动。
- 湿沙 / 湿草：雷泽开启时走速 **×0.88**（沙脊叠在 0.55× 上）。水里仍用月池/断崖原倍率，不再叠。飞步 / 影挪轨迹不变。
- HUD 顶栏芯片 **「雷泽」**，只在本局开启时显示。不把地图改名。

### 5.4d 余震（v3.3）

大坑落地后的 juice。名字 **余震**。不是新图、不是第九把枪，不写传送 / 飞行 / 三叉戟 / 激怒。

- 触发：本次坑半径 **≥ 36**，或武器是 **高爆 / 三裂 / 迟雷**（含迟雷粘地后爆）。
- 时长约 **0.28s**。场上只 **一道**；第二发大坑把计时刷新到 0.28，不叠第二道。
- 镜头抖 **3–5px**。`prefers-reduced-motion` **不抖**，土屑仍可少画。
- 坑沿再落 **4–7** 粒土屑（沿用殿塌碎屑，只画不伤）。
- 单位若站在爆心 **90px** 内、脚下土 **< 28px** 的薄沿上：**50%** 朝坑滑 **8–14px**。只改 x，不瞬移进坑；随后贴地 / 坠落，可埋。
- 不吐司。时钟闪光非必须。画布边缘一层薄土尘即可。
- **不替换** v1.7 直击 140ms 顿帧 + punch。余震是 `explode` 之后的加料。
- 不改 `GRAV` 260、`VK` 420、地图、武器表。overlay 仍 `pointer-events: none`。

### 5.4e 落顶（v4.5）

洞顶被真正啃石后的二次伤害。名字 **落顶**。不是第九把枪，不是三叉戟，不写传送 / 飞行 / 激怒 / 天使 / 恶魔。只在 **洞顶**。

- 触发：`carveCave` 返回 true（洞顶 / 牙真正吃到石）。地板坑不刷。
- 从击点落下 **2–4** 粒石屑，外形沿用洞顶碎屑（STONE 圆点）。每粒短命抛体：重力 **260**、无风、半径约 **4px**。
- 出生 **0.55s** 内碰到活人：该单位 **3** HP（堂核 ×1.15），小 punch，不埋、不吐司；HP 到 0 才走击破 / 结束层。同一爆破每人只吃一粒。
- 石屑碰土面 / 地板即消失。
- `prefers-reduced-motion`：**不刷**伤害石屑（装饰碎屑仍可少画）。
- AI 不瞄准石屑。其他十九图不变。大坑仍吃 **余震**。堂袋 / 堂匣 50% / HUD `2发×1.35` / 八枪 / 二十图不改。
- overlay 仍 `pointer-events: none`。`GRAV` 260、`VK` 420。

### 5.4f 袋火（v4.7）

×2 / ×3 齐射的拖尾 juice。名字 **袋火**。不是第九把枪，不是三叉戟，不写传送 / 飞行 / 激怒 / 天使 / 恶魔。

- 触发：开火时 ×2 或 ×3 武装。仅 +N 不刷这条拖尾（炮口喷仍在）。
- 该齐射 **每一壳**（含后续 0.32s、叠珠第二壳）画短拖尾（约 12 点）+ 屑。色用 `BAG_TINT`：×2 金 `#ffe36b`，×3 绯红 `#dc143c`。
- 若同时武装 +N：屑按约 32% 混 plus 色（奶油 / 橙 / 猩红 / 金箔）。
- 后续发略亮于第一发，读成「袋在打」。
- 炮口混色喷（v4.1）保留。`prefers-reduced-motion`：**跳过拖尾与屑**，炮口喷仍跳过。
- 不改叠算、体、互斥、八枪、二十一图、落顶。HUD `2发×1.35` 仍在。堂匣 50% 不改。
- overlay 仍 `pointer-events: none`。`GRAV` 260、`VK` 420。


### 5.4g ×3 同时分裂（v4.8）

×3 不再 0.32s 连打。一发松手，三壳同膛出门：`ang-8°` / `ang` / `ang+8°`（夹 0–180），每壳伤/溅/坑 ×0.60。名字仍 **×3**，禁止写三叉戟。×2 仍是后发（0.32s、±1°）。叠珠 + ×3：只裂本发三壳，不排队叠珠第二发。袋火三壳都画绯红拖尾。HUD `3发×0.60`。体不够卸武装，普通弹照打。overlay `pointer-events: none`。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

### 5.4h 金匣袋（v4.9）

金匣额外掉堂袋。名字 **金匣袋**。不是第九把枪，不是三叉戟，不写传送 / 飞行 / 激怒 / 天使 / 恶魔。

- 金匣仍 **每次** +28 怒，吐司「金匣」。`maybeBagCrate('gold')` 仍返回 `'gold'`，不把金匣改成袋匣。
- 另 **40%**（`GOLD_BAG_P`）再 +1 堂袋。袋物加权偏 ×2 / +5 / +3，高于 回春（`GOLD_BAG_W`）。
- 双掉：两段短吐司，先「金匣」，再「金匣 · ×2」（或 +5 / +3 等）跟 `BAG_TINT`。只怒则仍单条「金匣」。
- 非金堂匣改发袋 **50%** 不改（仍偏 ×2 / +3 / +2）。
- 不改叠算、体（含 0 体卸武装照打）、×3 同时分裂、袋火、落顶、八枪、二十一图。
- overlay 仍 `pointer-events: none`。`GRAV` 260、`VK` 420。

### 5.4i 蓄条（v4.12）

蓄力中的模拟条。名字 **蓄条**。不是第九把枪，不是三叉戟，不写传送 / 飞行 / 激怒 / 天使 / 恶魔。不改 `CHARGE_T`。

- 只在对局 `phase==='charge'` 且正在蓄力时显示。挂在 HUD「力 N」旁，短条。数字仍每帧写 `力 N`。
- 填充从 `TAP_POW`（空）线性到 100（满）。`CHARGE_T` 仍 **2s** 走满。
- 到 100：条与「力」标签金闪 **一次**；玩家仍可松手发射。
- 标题 overlay / 非对局：**隐藏**。`pointer-events: none`。
- `prefers-reduced-motion`：条照填，**不闪**。
- 不改物理、堂袋 +N 叠加 / ×2×3 互斥、二十二图、八枪。
- overlay 仍 `pointer-events: none`。`GRAV` 260、`VK` 420。

### 5.5 十格尺（v0.2）

- 从 **当前行动单位的脚 x** 起，沿水平每 48px 一根竖线，左右都画，最多到 20 格或画布边。
- 线高 22px，贴在该 x 的地面上方 4px。颜色 `rgba(255,227,107,0.35)`。每 5 格一根稍亮、写数字。
- 当前瞄准落点口诀格（65° 表按力度反查，或 90° 按 |90−elev|）高亮为 70% 不透明。
- 格尺 **不是** 弹道预测。它不弯，不吃风。
- `prefers-reduced-motion` 下仍画，只是不闪。
- 默认开。写入 `playbox-tan-tang-best.ruler`（bool，缺省 true）。

### 5.6 小地图（v0.3，写清以免提前做歪）

- 画布右上，160×90，半透明。地形剪影 + 青/洋红两点。
- 不接收点击（`pointer-events: none`）。
- v0.2 **禁止** 做，避免和格尺抢位置。

### 5.7 Overlay 与点击穿透（已上架，必须保持）

`.overlay` 与 `.overlay.hidden` 均为 `pointer-events: none`。只有 `.panel` 内按钮是 `pointer-events: auto`。  
结束层、标题层 **绝不挡住顶栏重开 / 静音**。R 在 overlay 打开时仍可用。  
程序员新加的道具条、格尺、残影、演习场表、障幕雾盘，全部挂在 `.stage` 里或顶栏或画布，不要做第二个全屏吃点击的层。

### 5.8 底栏 hint

v0.1：`走位 · 拉角 · 空格蓄力松手打 · 65° 最远 · 90° 落脚边 · 读风`  
v0.2 对局：`Q飞步 E影挪 C霓弹 V鼓息 B逆息 G障幕 F殿破 X过 · 65°查表 · Tab尺 · K残影`  
演习场：`无对手 · 表随距离变 · 空格仍能打木桩`

---

## 6. 属性

### 6.1 HP

| 模式 | 上限 | 开局 |
|---|---|---|
| 弹堂 | 100 | 100 |
| 堂核 | 60 | 60 |
| 演习场 | 岚丸 100，木桩 9999（不显示击破） | 同左 |

扣到 0 死亡。没有护盾、没有再生。v1.0 以前不做吸血。

### 6.2 体力（v0.2 新增；v0.1 只有「步 96」）

每回合开始：`stamina = 100`。不跨回合储存。

| 行为 | 体力 | 额外限制 |
|---|---|---|
| 走路 1 px | −1 | 本回合走路最多 96 px（与 v0.1 相同手感） |
| 飞步 | −35 | 不足 35 不能放；每局限 2 |
| 影挪 | −25 | 不足 25 不能放；每局限 1 |
| 霓弹 | 0 | 占本回合射击 |
| 鼓息 | 0 | 即时 |
| 逆息 | −15 | 不足 15 不能放；每局限 1 |
| 障幕 | −20 | 不足 20 不能放；每局限 1 |
| 殿破 | 0 | 即时点亮 |
| 跳过 / 冻结跳过 | 0 | — |

走路速度保持 v0.1：玩家 90 px/s，AI 78 px/s。`walk` 预算与体力 **同时减**：`step = min(walkLeft, stamina, speed*dt)`。  
被冻结的回合：体力回满但立刻跳过，等于没用。  
HUD「体」显示 `stamina` 整数。体=0 仍可射击、可鼓息、可跳过、可殿破，只是不能走、不能飞步/影挪/逆息/障幕。

### 6.3 怒气 0–100（v0.2）

整数。开局 0。上限 100。死亡清零。重开清零。

| 事件 | 怒气 |
|---|---|
| 对敌造成伤害 | `+ floor(dmg * 0.45)` |
| 自己受伤（含摔、溅射、自伤） | `+ floor(dmg * 0.55)` |
| 坠亡（致死那一下） | +20 然后死亡，来不及开殿破 |
| 打空 | 0 |
| 鼓息 | +50 |
| 连堂 | 不加（连堂只是表现） |

加减后 `clamp(0,100)`。满 100 顶栏怒条金闪 + toast「怒满 · F 殿破」（只提示一次，用 `ragedPrompted` 标记）。

2v2 时怒气 **按单位独立**。

### 6.4 暴击（v1.0，现在不要做）

预留字段 `critChance=0`。v1.0 方案：8% 使本次 `hurt` ×1.5，飘字「暴」。殿破与暴击可叠（先 ×1.6 再 ×1.5）。v0.2 / v0.3 暴击率必须保持 0。

### 6.5 延迟与出手顺序（仅 2v2：对堂 / 堂座）

1v1 **不要** 做延迟条（会把节奏做慢）。2v2 才用。

开局 delay：

| 单位 | 侧 | delay |
|---|---|---|
| 岚丸 | 左 | 0 |
| 烬丸 | 右 | 8 |
| 霜丸 | 左 | 16 |
| 霆丸 | 右 | 24 |

每回合：选 delay 最小者行动；并列按上表从上到下。  
行动结束（射击落地 settle 完，或跳过）：

```
delay += 武器延迟
```

| 行为 | 加延迟 |
|---|---|
| 普通弹 | +100 |
| 高爆 | +130 |
| 穿透 | +110 |
| 三裂 | +120 |
| 霓轨 | +140 |
| 霓火 | +125 |
| 叠珠 | +135 |
| 迟雷 | +120 |
| 霓弹 | +90 |
| 时尽（到 0 过） | +80 |
| 跳过 | +80 |
| 殿破（随这发结算） | 武器延迟再 +20 |
| 飞步 / 影挪 / 鼓息 / 逆息 / 障幕 / 殿破点亮 | +0（不结束回合） |

风：每个 **单位行动开始** 重掷一次（与 1v1 每回合重掷同频率）。  
胜利：对方两人都 HP≤0。先倒的人从顺序里摘掉。

---

## 7. 武器

开局三把都有，无限弹药。每回合只能打一发（霓弹算那一发）。堂核伤害 ×1.15。殿破再 ×1.6 并放大坑。

### 7.1 已上架三把

| 武器 | 键 | 直击 | 溅射半径 | 坑半径 | 弹速系数 | 延迟(2v2) | 特效 |
|---|---|---|---|---|---|---|---|
| **普通弹** | 1 | 32 | 36 | 30 | 1.00 | +100 | 基准。青焰小爆 |
| **高爆** | 2 | 24 | 56 | 48 | 0.88 | +130 | 坑大、溅射远、直击略低。改地形、砸台沿、逼摔 |
| **穿透** | 3 | 30 | 32 | 26 | 1.06 | +110 | 第一次碰地形不爆，沿速度掏约 46px 隧道再穿出；第二次碰地形或穿出后 0.18s 才爆。只穿一次 |

溅射伤害用 §4.9 公式，基数是 `direct` 不是单独的 splash 伤害。`splash` 列是半径。

穿透实现要点（已上架，保持）：

- 第一次 `inGround`：`pierced=true`，`fuse=0.18`，沿单位速度方向立刻位移 46px，并用半径 11 的小圆每隔 4px 掏一串；若仍在地里，再每次 3px 最多 18 次拱出地面。
- 穿出后碰人立刻爆；再碰地立刻爆；fuse 扣完在空中爆。
- 爆炸时若已 pierced，再沿反方向补一串半径 12 的坑，让隧道看得见。

打中：粒子 + 顿帧 48–66ms + 镜头微震。打空：挖土音 + 土屑，不顿帧。

### 7.2 路线武器（名字锁定，数值如下，v0.3 先做一把）

禁止改回任何外部游戏的武器名。

#### 三裂（v0.3，已上）

键 **4**。一发飞到落地，**在爆心炸成 3 个紧簇小坑**（不是空中分裂）。每坑 direct 14 / splash 22 / 单坑半径 18–22，伤低于高爆；三坑沿深度叠挖，总坑深于高爆，一发可埋。弹速系数 0.96。延迟 +120。无限弹，每回合一发。顶栏 + 触屏「裂」。AI：对方在薄台或浅坑时选用。

#### 霓轨（v0.4，已上）

键 **5**。弹速系数 0.70，直击 20，溅射 40，坑 22。弹在空中每 0.05s 把 **当前风** 再乘一次 1.15（仍不改 G.wind，只改这发的水平加速度系数：`acc = wind * 2.05 * 1.15^(ticks)`，ticks 上限 8）。弧线被风明显拽弯，用来打掩体后。延迟 +140。顶栏 + 触屏「轨」。AI：`|wind|≥4` 时选用（坑里叠珠、台沿三裂仍优先）。

#### 霓火（v0.5，已上）

键 **6**。直击 18，溅射 44，坑 20，弹速 1.00。坑明显弱于高爆。落地在爆心留一团火，半径 28，持续 **两个 settle**。站在火里（中心距<28）每回合 settle 开始时再烧 `8 * dmgMul`。自己踩到也烧。与殿破叠：点燃伤害也 ×1.6。延迟 +125。顶栏 + 触屏「火」。

#### 叠珠（v2.4，已上）

键 **7**。顶栏名 **叠珠**，短提示「两发」，触屏「珠」。一发占回合，打出 **两壳**（不能第三发）：

- 第一发：当前蓄力、当前角、当前膛口。弹体同普通弹（直击 32 / 溅射 36 / 坑 30 / 弹速 1.00）。
- 0.38s 后第二发：同角同膛，力度 **0.72×**，角再 ±1.2° 微抖（不完美叠在一起）。溅射半径与坑半径亦 **×0.72**。每壳独立碰撞 / 伤害 / 挖坑。
- 第一发已结束对局（对方倒下）则 **不刷** 第二发。
- 殿破点亮时两壳都吃 ×1.6 / 坑 ×1.35。延迟 +135。无限弹。
- AI：对方 **埋了** 或坐在坑里时选用（第二发收尾）。薄台仍三裂。

1–6 仍是 普通 / 高爆 / 穿透 / 三裂 / 霓轨 / 霓火。禁止改回外部双发名。

#### 迟雷（v2.7，已上）

键 **8**。顶栏名 **迟雷**，短提示「迟爆」，触屏「雷」。一发占回合。弹速 0.94，直击 26，溅射 **42**，坑 **34**。

- 第一次碰**地形**（不是人）：粘在落点，**1.6s** 后爆，可埋。飞弹阶段结束，雷自己计时，换手后仍在。
- 飞行中先碰人：立刻爆，溅射半径 **×0.85**，不粘。
- 每边最多 **一枚** 活雷。再打一发：旧雷熄（不爆、无坑），新发替换。
- 可见引信：脉动标 + 倒计时环。`prefers-reduced-motion`：静标，不脉动，环仍画。
- 殿破点亮时粘雷落地后仍吃 ×1.6 / 坑 ×1.35。延迟 +120。无限弹。
- AI：不当开局枪。石门敌将走进廊、沙脊敌将走进鞍时封路；已在廊/鞍仍用高爆。埋了仍叠珠。易档不用。
- 禁止写成传送 / 飞行或外部地雷原名。

### 7.3 切枪规则

aim 与 charge 可切。fly / settle 不可切。AI 在 `startAI` 时选定，演出期间不换。标题页 1/2 是模式不是枪。对局里 7 是叠珠，8 是迟雷。

---

## 8. 道具（v0.2 第一批，必须按此做）

四术。不是地上掉落。每局限次，**跨回合保留剩余次数，重开清零**。键 Q / E / C / V，顶栏或画布下一条道具条同步。触屏只点条，不要求虚拟键盘。

通用规则：

- 仅自己的 `phase==='aim'` 可用（霓弹装备后可在 charge 打出）。
- 次数为 0、体力不足、被冻结：按钮灰，按键吞掉并 toast 原因，例如「体力不足」/「本局已用完」。
- 道具 **不进商店、不升级、不随机**。两模式次数相同。堂核不减次数。
- 演习场：次数 **每发重置为上限**（方便练），木桩不会用道具。
- 名称、图标、toast 只用本文的中文。禁止用「传送」「飞行」「激怒」。

### 8.1 总表

| 术 | 键 | 体力 | 每局限 | 占射击？ | 结束回合？ | 延迟 |
|---|---|---|---|---|---|---|
| **飞步** | Q | 35 | 2 | 否 | 否 | +0 |
| **影挪** | E | 25 | 1 | 否 | 否 | +0 |
| **霓弹** | C | 0 | 2 | **是** | 打出后是 | +90 |
| **鼓息** | V | 0 | 1 | 否 | 否 | +0 |
| **逆息** | B | 15 | 1 | 否 | 否 | +0 |
| **障幕** | G | 20 | 1 | 否 | 否 | +0 |

### 8.2 飞步

**做什么**：原地起跳，沿一条固定本地抛物横移，用来上坑沿、躲脚下、跨细缝。不是自由飞，不能在空中射击。

**轨迹（必须按此，不要吃风）**：

```
dir = 当前 face（+1 右 / −1 左）。若 aim 时正按着反向键，dir 取该键
dx = 80 * dir          // 水平 80 px = 1.67 格
arcH = 56              // 弧顶相对起跳脚的像素
时长 = 0.34 s
x(t) = x0 + dx * s
y(t) = y0 - 4*arcH*s*(1-s) + (groundAt(x(t)) - groundAt(x0)) * 0   // 先按抛物，落地再贴地
s = clamp(t/0.34, 0, 1)
```

落地：`x = clamp(x0+dx, 22, 938)`，若该 x 无地面（双台断层）则继续坠落，按普通重力，落差≥36 摔伤，掉出底边坠亡。  
弧顶 56、水平 80——文案写「跃弧 80」指 **水平跃距 80px**，不要做成 80px 高。

**流程**：扣 35 体力、次数 −1、`phase` 保持 aim 但加 `busy='leap'` 0.34s（此期间不走、不调角、不蓄力、超时钟 **不停**）。落地 `ungroundIfAir`。toast「飞步」。青焰残影 6 帧。

**不能**：体力<35；次数 0；已经在空中；charge / fly / settle。

**AI 规则**（§12.3 详）：

1. 先按现在位置 `solveAI` 得 score0。
2. 试 `x+80`、`x-80`（夹紧），落地后 `solveAI`。
3. 若某点 score > score0 + 220 且体力≥35 且次数>0，**先放飞步再走剩余步**（走是微调，飞步是跳跃）。
4. 自己在坑深≥40 且左右 80px 有更高地面时，100% 用飞步逃（即使分数不涨）。
5. 堂核少想 1 拍：若 HP≤18 且对方怒≥80，优先飞步拉开。
6. 否则不用。AI 每局最多用 2，与玩家相同。

### 8.3 影挪

**做什么**：指定一个点，自己立刻出现在那里。用于出坑、换台、拉开直线。**不要写「传送」**。

**距离**：从脚到目标的欧氏距离，上限 **220 px**（4.58 格）。超出则沿该方向夹到 220。  
**落点**：`x` 夹到 22–938。`y = groundAt(x) - 14`。若目标 x 落在断层（ground 接近 532 且左右 20px 都是断层），允许落，随后坠落——这是合法自杀/逼自己换位。  
**不穿画布顶**：y 最小 24。

**操作**：

1. 按 E 或点「影挪」→ `busy='warpAim'`。画布上画半径 220 的淡青圆，准星随指针。超时钟不停。
2. 再点画布 / 再按 E：确认。扣 25 体力、次数 −1，人闪到落点，0.12s 青残影。回 aim。
3. ESC / 右键：取消，不扣次数、不扣体力。
4. 触屏：点「影挪」进点地，再点画布确认。没有第二颗确认键。

charge 中不能进影挪。飞步 busy 中不能进。

**AI 规则**：

1. **埋了**：头上 20px 仍 `inGround`，或所在坑相对左右 48px 平均地面深 ≥40px → 在 220 内找地面最高且距敌 ≥ 3 格的点，立刻影挪。
2. **薄台**：自己脚下左右 36px 地面落差>28 且 HP≤35 → 挪到最近的厚台（列高度左右差<10 的平顶）。
3. **抢位**：`solveAI` 在 220 圆内 8 个均匀角度 × 距离 80/150/220 共 24 点采样，若最佳分 > 现在 + 400，才用（影挪每局只有 1 次，AI 要吝啬）。
4. 第一回合不用影挪。
5. 否则不用。

### 8.4 霓弹

**做什么**：本回合的射击变成一发冻结弹。命中（直击或溅射伤害≥1）则给对方 **冻结 1 个完整行动**。  
替换射击，不是额外动作。按 C 装备 / 再按 C 卸回原来武器。HUD 武器位旁亮「霓」标，不占用 1/2/3 的三把枪（三把仍在，霓弹是覆盖层）。发射后覆盖层清掉。

**弹道**：完全复制 **普通弹** 的 spd / loft（1.00）。  
**伤害**：direct **8**，splash 半径 **28**，坑半径 **18**。堂核 ×1.15，殿破 ×1.6。  
**冻结判定**：`applyBlast` 对 **敌方** 实际扣血 ≥1（自杀冻不住自己）。打空不冻。  
**冻结效果**：

- 标记 `frozen=1`。
- 轮到被冻单位时：toast「×× 被冻结」，重掷风，停 0.55s，`frozen=0`，**不走路不射击**，直接换下一个行动者。
- 被冻单位本回合不能用道具。
- 岚丸被冻：时尽钟不要走到过——直接冻结跳过。
- 连堂：霓弹命中算有效。

次数：每局 2。装备但没打出不扣次；**打出** 才 −1，即使打空。次数 0 时按 C 无效。

**AI 规则**：

1. 预测：用普通弹轨迹 + 霓弹伤害表，若能命中（score 按直击 12000 / 溅射 5000 同一套） **并且** 满足下面任一，才装备霓弹：
   - 己 HP − 敌 HP ≥ 15（领先，想偷一回合挖地或拉开）；
   - 敌怒气 ≥ 80（打断殿破）；
   - 敌站在薄台或坑沿，下一发高爆能挖摔，需要先让他不能走。
2. 敌 HP≤12 时 **不要** 霓弹，改普通/高爆发死。
3. 每局 AI 最多用 2，且连续两回合不用霓弹（避免冻到无聊）。
4. 打不准（最佳分<4000）不用。

### 8.5 鼓息

**做什么**：立刻 +50 怒气（夹到 100）。自己喘一口金光。**不要写「激怒」**。

- 按 V / 点条。扣次数 1。体力 0。不占射击，本回合仍可走、可打、可再飞步（若还有体力和次数）。
- 已满 100：仍可按，次数照扣，怒气不变，toast「怒已满」。AI **不要** 这样浪费。
- 每局限 1。

**AI 规则**：

1. 回合开始、还没走之前：若 `rage <= 50` 且本回合不打算影挪（影挪条件不成立）→ 立刻鼓息。
2. 若 `rage >= 51` 不用（一次 +50 会溢出浪费超过 1 点就忍住；51–99 时只有当 100−rage≤50 且准备本发殿破才用——即 rage≥50 且 solve 认为能打中，用完立刻 F）。
3. 简化实现：`rage<=50` 开回合必用；`50<rage<100` 且命中分≥8000 且用完能到 100 → 用；否则不用。
4. 第一回合可以用（让玩家早见殿破压力）。

### 8.6 道具条 UI

```
[ 飞步 2 ] [ 影挪 1 ] [ 霓弹 2 ] [ 鼓息 1 ] [ 逆息 1 ] [ 障幕 1 ] [ 过 ]
     Q          E          C          V          B          G        X
```

宽 ≥36px，角标在右上。用完 `opacity:0.35; pointer-events:none`。霓弹装备中按钮 `.on` 青边。  
桌面顶栏武器钮左边或下一行；窄屏（画布<700）只留画布下那条。  
`pointer-events` 只开在按钮上，条底板 `pe-none`。

### 8.7 殿果（v0.8）

飞行中空悬浮珠。**不是地上掉落**。名字 **殿果**。

- 模式：弹堂 / 堂核 / 对坐 / 对堂 / 堂座。演习场不刷。
- 时机：`fire` 后 `phase==='fly'`。每发最多 2 颗。随机 0 / 1 / 2（约 34% / 48% / 18%）。
- 对局岚丸第一回合（`turns<=1` 且 `turn==='p'`）不刷。
- 位置：随机半空，距地 ≥56px，距残墙 ≥36px，距人 ≥80px，互距 ≥72px。软光悬浮。
- 判定：弹心扫过距果心 ≤ `FRUIT_R+6`（12+6）。拾取不炸弹，弹继续飞。
- 普通殿果：随机给行动者 +1 飞步 / 影挪 / 霓弹 / 鼓息。该术次数不超过开局上限 +1。toast「殿果 · 飞步」等，粒子爆开。
- 金殿果：约 15%。一次 +25 怒（夹到 100）。toast「殿果 · 怒」。
- 未拾：本发 settle 完、换手前消失。
- 烬丸：同一套碰撞。求解器不找果。

### 8.8 障幕（v1.4）

**做什么**：在当前单位落下半径 **70px** 的青雾盘。不是地上掉落。名字 **障幕**。

- 键 **G**。顶栏 + 触屏「幕」。每局限 1。体力 **20**。不占射击，不结束回合，延迟 +0。
- 落点：按下去那一帧的单位中心，不跟随人走。
- 持续：直到 **下一次敌方行动** 结算完（射击落地 / 跳过 / 冻结跳过）。己方行动不消耗。对堂延迟：队友出手雾还在，敌方一人出手后消散。演习场无对手，本发 settle 后消散。
- 雾内：
  - 对方的辅助 **落点叉圈** 隐藏（弧仍可画）。自己 / 同队仍看得见落点。
  - AI 打雾里的人：瞄准误差 **+12%**（角抖与力抖都 ×1.12）。自己这发不受。
  - 单位精灵仍画，自己看得见自己。
- 视觉：画布上旋涡青雾，无第二层 DOM。`pointer-events` 不挡顶栏。
- 吐司「障幕」。用完或体不够：灰钮 + 原因。
- 不进殿果池。重开清零。演习场每发重置次数。
- **AI**：HP < 40 且与目标水平距 **< 8 格** 才放；放完停在雾里再打，不走出盘。

### 8.9 堂匣（v2.9）

场上木匣。名字 **堂匣**。金变体 **金匣**。不是第九把枪，不是殿果，不写传送 / 飞行 / 三叉戟 / 激怒。

- 模式：弹堂 / 堂核 / 对坐 / 对堂 / 堂座。演习场不刷。
- 时机：settle 完、`continueAfterAction`、下一手瞄准前。每次 0–1 只从天落下。场上最多 **2** 只，跨回合保留，直到打爆或走进拾走。
- 开局第一回合（`turns < 1`，即第一发 settle 前）不刷。
- 落点：可走土面。不落霜泽冰、云台石板/缝坑、镜廊墙/沟、井口井内/石唇、洞顶坑、齿岸峰/谷、环坑沿/碗、深坑（相对左右 48px ≥40px）、虚空、熔浆、月池水面、断崖浅水、残墙实心。远离人 48px、匣距 ≥56px。
- 视觉：小木箱。默认带一下落弹跳。`prefers-reduced-motion`：**仍刷**，直接贴地，无弹跳。无第二层 DOM。overlay 仍 `pointer-events: none`。
- 摧毁 / 拾取（同一张表）：
  - 弹 **直击** 或 **溅射** 半径内 → 给 **开枪者**。
  - 走进（身体碰到匣）→ 给 **走路的人**。飞步落地 / 影挪落点同样。
  - 50%：+1 飞步 / 影挪 / 霓弹 / 鼓息（只在还能持有的里抽，上限同殿果 = 开局 +1）。吐司「堂匣 · 术」。
  - 35%：+18 怒（夹到 100）。吐司「堂匣 · 怒」。
  - 15%：**金匣** +28 怒。吐司「金匣」。
  - 术已满则改发 +18 怒。
- 殿塌（`G.sudden`）期间刷率约 **42%**，平时约 **28%**。
- 地形被挖空或坠入虚空：匣掉下去，不发奖。
- AI：匣比当前敌人更近、且自己 HP≥40、且本发不是直击/残血击杀时，朝匣多走 **36px**。绝不放弃击杀去追匣。求解器不把匣当目标。
- 不改 1–8 武器、十六图、`GRAV` 260、`VK` 420。
- v3.6 / v4.0：非金匣时改发 **堂袋** 随机 +1（×2 / ×3 / +1 / +2 / +3 / +5 / 回春），术/怒表其余仍旧；**金匣** 不改。
- v4.3：该改发概率 **50%**。袋物加权偏 ×2 / +3 / +2，略高于 回春。吐司「堂匣 · ×2」跟 `BAG_TINT`。
- v4.9 **金匣袋**：金匣仍 +28 怒。另 **40%** 再 +1 堂袋（偏 ×2 / +5 / +3，高于 回春）。双掉先「金匣」再「金匣 · ×2」。`maybeBagCrate('gold')` 仍 `'gold'`。非金 50% 不改。

### 8.10 堂袋（v4.0 / v4.1 / v4.2 / v4.3 / v4.7 / v4.8 / v4.9）

画布下底栏道具托盘。名字 **堂袋**。不是第九把枪，不写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。**叠珠**仍是武器 7，不是 ×2。

- 位置：`.app` 内、`.stage` 下、页脚上。`#item-dock` 与格子 `pointer-events: auto`。overlay 仍 `none`。
- 7 格（40 viewBox 厚描边宝石卡，36px 可读；v4.1 街机 glyph：厚描边 + 宝石框 + 内高光）：×2 `x2`（金：额外 1 发，共 2 壳，每壳伤/溅/坑 ×0.90，开火 −40 体）、×3 `x3`（绯红：一发三壳同膛分裂，角 ±8° 夹 0–180，每壳 ×0.60，开火 −40 体；不再 0.32s 连打）、+1 `p1`（奶油：伤 +10%，−20 体）、+2 `p2`（橙：+20%，−20 体）、+3 `p3`（猩红：+30%，−25 体）、+5 `p5`（金箔：+50%，−40 体）、回春 `heal`（叶；点按立刻 +14 HP，−15 体，封顶）。
- 叠加：×2 与 ×3 **互斥**（武装一个卸另一个）。+1/+2/+3/+5 可同时武装，百分数相加：`plusMul = 1 + 0.10*n1 + 0.20*n2 + 0.30*n3 + 0.50*n5`。每壳因子 `(x2?0.90 : x3?0.60 : 1) * plusMul`。经典 ×2++5 ≈ 2 × 0.9 × 1.5 = 2.7×。
- 体：走路也耗体。开火时若剩余体 < 武装总价，吐司「体不够」并卸武装，**普通弹照打**。回春点按仍耗体、体不够不回春。武装物开火消耗（回春点按消耗）。
- 每人独立袋。开局 2，演习场 3。芯片行「×2++3++2」，每枚跟图标同色。空槽 0.12s 晃。武装金斜面 + 火花（v3.7 木石托盘保留）。
- v4.2 力旁 readout：瞄准 / 蓄力且有武装时，`#bag-aim-tag` 写 `2发×1.35`（`extra+1` × `shellMul`，两位小数）。仅 +N 无 ×2/×3：`+50%`（plusMul 1.50 同义）。体不够加 `.broke` 变红；吐司「体不够」不改。芯片行仍在。
- v4.1 开火 juice：若本发有武装物，炮口短暂混色喷（叠了哪些就喷哪些 tint）；`prefers-reduced-motion` 跳过喷。消耗格 0.18s 闪。不改叠算、体、互斥。
- v4.7 **袋火**：×2 / ×3 齐射每壳短拖尾（`BAG_TINT` 金 / 绯红）；叠 +N 则屑混 plus 色；后续发略亮。减动效跳过拖尾，炮口喷仍跳过。不是第九把枪。
- v4.8 ×3 **同时分裂**：一发三壳同膛出门（`bagForkAngles` ±8°），`bagExtraCount(x3)=0`。叠珠+×3 只裂本发，不排队叠珠第二发。名字仍 ×3。
- 键：`-` ×2、`=` ×3、`[` +1、`]` +2、`\` +3、`;` +5、`'` 回春。不抢 1–8、不抢 QECVBGFX。飞步等术仍在顶栏 `.items`。
- 堂匣非金 **50%** 改发这 7 枚之一（偏 ×2 / +3 / +2，略高于 回春）。金匣仍怒。吐司「堂匣 · ×2」跟 `BAG_TINT`。v4.9 金匣 **40%** 另 +1 袋（偏 ×2 / +5 / +3），吐司「金匣 · ×2」。
- AI 狠：敌暴露且体≥60 叠 ×2++3（无 +3 则 +2）；近距且体≥80 偏 ×3++2。永不叠 ×2 与 ×3。中：只 ×2，无 plus。易几乎不用。HP<40 可回春。
- `GRAV` 260、`VK` 420。二十图 / 八枪不改。

---

## 9. 必杀 — 殿破

怒气到 100，下一发强化。名字 **殿破**。不是单独的第六种武器。

### 9.1 点亮

- 条件：自己怒气 == 100，且 `phase==='aim'`，且还没点亮。
- 输入：F，或点怒条 / 「殿破」钮。
- 立刻：怒气清 0，`ult=true`，怒条空但仍描金边直到这发落地。toast「殿破 · 下一发」。
- 点亮不结束回合，仍可走、可换枪、可飞步/影挪、可把这发改成霓弹（冻+殿破允许，伤害 8×1.6）。
- 不能取消。跳过 / 被冻跳过：`ult` 清掉，怒气 **不退回**。
- 时尽到 0：`ult` 清掉，怒气不退回（与跳过相同，不打出）。

### 9.2 结算

`explode` 时若 `owner.ult`：

```
本次 hurt 的 dmg *= 1.6（在 dmgMul 之后）
坑半径 *= 1.35（普通 30→40，高爆 48→65，穿透 26→35，霓弹 18→24）
ult = false
```

额外：金色全屏闪 0.45、hit-stop 取 max(当前, 0.08)、镜头 kick 至少 6.5、飘字「殿破」。  
连堂照常。击破时沿用「堂破了」——注意两个词不同：殿破是必杀名，堂破了是胜利标题。

### 9.3 AI 开殿破

怒气 100 且本发命中分 ≥ 5000 → 蓄力前点亮。否则攒着（下回合还是 100）。不要在必空的一发上浪费。

---

## 10. 地图

虚拟场一律 960×540。三图同宽不同轮廓。地面可挖穿。掉出底边坠亡。高度夹在 88～532。

### 10.1 已上架三张

#### 平原 `plain`（默认）

```
h[x] = 394 + sin(t*π*2.05)*18 + sin(t*π*5.3)*8 + sin(t*π*11.2)*3.5
t = x/959
```

缓丘开阔。教学：65° 远射、读风。开局水平约 12.8 格 → 表上力 70 左右。

#### 峡谷 `canyon`

```
v = sin(t*π)^1.32
h[x] = 294 + v*206 + sin(t*16.2)*7 + sin(t*39)*3
两侧 64–196、764–906 垫平到 ≈306
```

中央深 V。教学：高抛或 **穿透** 穿壁。开局 x=122 / 838。

#### 双台 `twin`

左右 smoothstep 高台（约抬 198px），中央断层。开局 x=148 / 812。教学：90° 近打台沿；过缝要对风。掉进缝 = 很长的摔或坠亡。

标题页三钮，选中 `.on`。写入 `playbox-tan-tang-best.map`。对局中途不换图。R 重开同图。

### 10.2 计划三张（v0.3 一张，v1.0 两张）

做新图时 **只加高度图 + 局部风区 + 出生点**，不要加新系统。标题页地图钮变成最多 6 个，一行放得下就一行。

#### 风柱 `spire`（v0.3，已上）

中央 **上升气流柱**（约 x=400–560）：弹在柱内额外向上加速度（峰值约 −150 px/s²，叠在重力上），画闪光。只影响弹，不影响走位。出生 140 / 820。教学：弧线过柱会扬起来。

#### 碎桥 `bridge`（v0.3，已上）

左右高台 + 中央一座 **厚度 22px 的石桥**（x=360–600，顶 y≈310）。**高爆 / 三裂** 打在桥上会打断该段，落入缝。出生 150 / 810。AI 对方在桥上时优先三裂。

#### 悬岛 `isles`（v0.4，已上）

三块浮岛：左 80–280 顶 y≈360，中 400–560 顶 y≈240，右 680–880 顶 y≈360。岛外 `groundAt` 返回 532（即「没有地」，人走过去就坠亡）。飞步 80px 刚好能从左岛边缘跳上中岛边缘（间距约 120，所以要先走到边再飞步不够——必须 **影挪** 或先把岛炸近）。教学：道具位移。出生 160 / 800。标题页可点选。

### 10.3 地图选择规则

`MAP_IDS` v4.10：`['plain','canyon','twin','spire','bridge','isles','ruins','vale','forge','arcade','towers','moon','cliff','dune','gate','frost','cloud','mirror','well','cave','teeth','ring']`。  
加图只 append，不要改旧 id。未知 id 回退 plain。  
演习场二十二张都能练；风柱 / 风谷过柱改弧，65° 表仍给出柱后口诀。随图从 `MAP_IDS` 抽。

#### 残垣 `ruins`（v0.5，已上）

左右各一堵残墙作掩体（约 x=288–328 与 632–672，高约 110）。挡弹；走进实心段会停，站墙后躲平射。**高爆 / 三裂** 打在墙上会打出圆洞；其它枪撞墙即爆不打洞。出生 140 / 820。教学：借墙躲平射，用高爆拆。标题页可点选。

#### 风谷 `vale`（v0.7，已上）

比峡谷更深的 V。两侧垫平约 y≈286，谷底接近底边。中央升流约 x=384–576，峰值 **−240 px/s²**（风柱是 −150），只吹弹不吹走位。出生 128 / 832，都在岸台。教学：65° 高抛吃升流扬过谷。v2.5 开局必开 **雷泽**。

#### 熔台 `forge`（v0.7，已上）

左右高台（约 x=36–252 与 708–924，顶 y≈268）中间一条薄壳（x=292–668，顶 y≈294，厚 16px），壳下熔坑。台/壳外 `groundAt` 为 532，坠入即死（碰到熔面 y>400 也清零）。任何坑打穿薄壳即塌成熔隙。飞步 80px 刚好从台沿跳上薄壳。出生 148 / 812（高台上，安全）。教学：90° 砸壳/砸沿，飞步过缝。标题页可点选。v2.5 **永不** 开雷泽。

#### 廊桥 `arcade`（v1.3，已上）

两侧高岸（约 x=28–248 与 712–932，顶 y≈310）中间一座 **长拱石桥**（x=248–712，拱顶 y≈180，厚 28px）。厚可站人；**高爆 / 三裂** 打在拱上砸穿该段，坠入拱下虚空即死。普通弹只砸凹，不塌洞。出生 140 / 820，都在岸上，不在拱上（拱是争夺位）。对堂四人仍错开在两岸。教学：65° 越拱打对岸，30° 穿拱砸桥面。标题页可点选，随图含廊桥。

#### 双塔 `towers`（v1.5，已上）

左右两座粗石塔，内沿相距约 **180px**。平顶可站（出生 186 / 774）；内沿矮台可巢。中间庭院可落地，坠塔只吃摔伤，无熔浆、不当即死虚空。内沿垛墙：**高爆 / 三裂**啃垛口；**穿透**开窗。对堂霜丸 / 霆丸站内沿巢台。教学：90° 砸顶 / 砸沿，65° 对射过缝。标题页可点选，随图含双塔。

#### 月池 `moon`（v1.9）

中央一座 **圆坑湖**。沿高（顶 y≈246），对沿出生 174 / 786；**65°** 从沿打到对沿是这张的学校。坑底是水不是即死虚空：站在水面下慢走 **0.45×**，**回合开始**若仍在水里伤 **3**。对堂霜丸 / 霆丸沿沿略错开（仍在干沿上）。**高爆**砸在沿上会把那一截塌进池（普通 / 三裂只挖凹）。教学 65° 越池打对沿。标题页可点选，随图含月池。

#### 断崖 `cliff`（v2.1）

左高台（顶 y≈250）右低滩（y≈430），中段 **绝壁** 落差约 **180px**（约 x=508）。坠崖只吃摔伤，不是即死虚空。崖脚浅水（约 x=520–632）：慢走 0.45×，回合开始浸 **2**（月池是 3）。出生 168 / 772，左台 / 右滩，都在干地上。对堂霜丸 / 霆丸同面略错开。教学：台沿 **90°** 砸滩埋人；滩上 **30°** 掏壁。标题页可点选，随图含断崖。v2.5 开局必开 **雷泽**。

#### 沙脊 `dune`（v2.3）

两侧 **高沙丘**（顶 y≈246，出生 168 / 772 在丘顶），中央 **浅鞍**（y≈394）。全图沙地：慢走 **0.55×**（`onSand`，按图 + 沙色地面）；弹坑半径约 **×1.25**，埋人更容易。风掷比默认 max **±1**，再夹 `windMax()`（弹堂仍 ≤8，堂核仍 ≤14）。对堂霜丸 / 霆丸同丘面略错开（204 / 736）。教学：丘沿 **90°** 砸鞍；**30°** 掏对丘内坡。AI 对方在鞍里时偏高抛砸。标题页可点选，随图含沙脊。风丝 / 残影 K / 断崖 / 月池不改。v2.5 开局必开 **雷泽**（湿沙再 ×0.88）。

#### 石门 `gate`（v2.6）

两座 **石柱**（左约 x 280，右约 x 660）夹一条 **中缝廊**。外沿土台出生 **168 / 772**；柱冠可站、满速；廊底是坑，坠入可埋、非即死虚空。柱石更硬：爆心落在柱 x 带上坑半径 **×0.72**，土台仍原坑。教学：**30°** 穿缝，**90°** 砸柱冠，**65°** 常削柱沿。对堂霜丸 / 霆丸同外沿略错开（204 / 736），不进门缝、不站柱里。AI：敌在廊里偏 **90°** 砸或 **高爆**；敌在对冠偏 **65°**。雷泽约 **35%**（不是必开）。标题十五图，随图含石门。叠珠 / 风丝 / K 残影 / 断崖 / 沙脊不改。`GRAV` 260、`VK` 420。

#### 霜泽 `frost`（v2.8）

两侧 **土岸**（顶 y≈304，出生 **168 / 772**）中间一座 **宽平冻湖**（y≈408，约 x 268–692，苍白冰面）。冰上走 **1.15×**，松手仍滑（残 `vx` 慢衰，反向难刹）；岸上满速即停。弹打冰、着角相对冰面 **< 28°**：**跳一次**（`vy` 反弹 ×0.55，`vx` ×0.92，小火花），第二次碰地才爆；陡击（≥28°）照常爆。冰硬：坑半径 **×0.55**；岸上原坑。对堂霜丸 / 霆丸同岸略错开（204 / 736），不站湖心。AI：敌在湖上偏 **65° / 90°** 砸冰（不跳）；近岸唇用 **迟雷** 封退路。雷泽约 **35%**（不是必开）。标题十六图，随图含霜泽。叠珠 / 迟雷 / 风丝 / K 残影 / 断崖 / 沙脊 / 石门不改。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒。

#### 云台 `cloud`（v3.1）

两侧 **土岸**（顶 y≈308，出生 **168 / 772**）中间一道深坑（y≈468，非虚空），坑上有一块约 **220px** 宽的 **石板** 在缝里左右滑：振幅约 **90px**，周期约 **7s**。站在板上的人继承板的水平速度（跟着走）；从板沿掉下去吃摔伤 / 可埋，不是即死虚空（掉出画布底才坠亡）。弹打在板上坑半径 **×0.8**（薄石）；岸上原坑。板被挖到剩余过低则 **停住** 变成残石，不再滑。对堂霜丸 / 霆丸同岸略错开（204 / 736），开局不站板。AI：敌在板上时瞄准提前量 `x + vx × tof`，偏 **65°** 打到板上。雷泽约 **30%**（不是必开）。标题十七图，随图含云台。叠珠 / 迟雷 / 堂匣 / 时尽 / 风丝 / K 残影 / 断崖 / 沙脊 / 石门 / 霜泽不改。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒。

#### 镜廊 `mirror`（v3.2）

两侧 **土岸**（顶 y≈308，出生 **168 / 772**）中间一道 **浅沟**（y≈378，非虚空），沟里立一座约 **18px** 宽、**220px** 高的 **镜墙**。弹打墙面侧击 / 擦边：`vx *= -1` **折一次**（小火花，无吐司），下一碰才爆。顶砸（冲击法线 |nx| 小）立刻爆并啃墙，坑半径 **×0.5**。剩高 **< 40%** 或实心柱太少则 **塌成残石**，不再折。走进墙被挡住（实心）；坠入沟摔 / 可埋，不是即死虚空。对堂霜丸 / 霆丸同岸略错开（204 / 736）。AI：敌在对岸时，若折弹落点更近则偏 **40–55°** 瞄墙；否则 **65°** 越墙（墙还立着）。雷泽约 **30%**（不是必开）。标题十八图，随图含镜廊。叠珠 / 迟雷 / 堂匣 / 时尽 / 云台 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒。

#### 井口 `well`（v3.4）

两侧 **土岸**（顶 y≈308，出生 **168 / 772**）中间一口约 **240px** 宽的 **深井**（底 y≈498，非虚空），底有暗水盘。石唇略高于岸。教学：**90°** 从岸唇砸进井；**30°** 越唇打对岸；**65°** 常削近唇。井内走 **0.40×**，**回合开始**浸 **4**。陡壁走不出去，炮弹砸出缓坡才能爬，或 **飞步** 越唇。石唇坑半径 **×0.75**；底泥 **×1.15**（大坑仍吃余震）。对堂霜丸 / 霆丸同岸略错开（204 / 736），开局不进井。AI：敌在井内偏 **90° / 高爆**；近出口 **迟雷** 封爬；对岸偏 **65°**。雷泽约 **30%**（不是必开）。标题十九图，随图含井口。叠珠 / 迟雷 / 堂匣 / 时尽 / 云台 / 镜廊 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒。

#### 洞顶 `cave`（v4.4）

两侧 **土岸**（顶 y≈340，出生 **168 / 772**）中间浅坑（约 x 400–560）+ 洞顶钟乳（牙尖约 y **210**，宽约 28，x≈360 / 600）。牙下可走，坑非虚空。教学：**90°** 砸顶；**65°** 削近牙；**30°** 越坑打对岸。洞顶 / 牙坑半径 **×0.70**（大坑仍吃余震）。砸顶 / 削牙若真正啃石，触发 **落顶**（v4.5）：2–4 粒落石，0.55s 内碰人 3 HP。对堂霜丸 / 霆丸同岸略错开（204 / 736），开局不进坑。AI：敌在牙下偏 **90° / 高爆**；对岸 **30°** 越坑；岸唇 **迟雷** 封进坑。不瞄准落石。雷泽约 **20%**（不是必开）。标题二十图，随图含洞顶。堂袋 / 堂匣 50% / 八枪 / 井口 / 余震 / 时尽不改。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

#### 齿岸 `teeth`（v4.6）

两侧 **土岸**（顶 y≈308，出生 **168 / 772**）中间四座 **石牙**（峰约 x 300 / 420 / 540 / 660，峰 y≈210，谷 y≈360，牙宽约 70px）。谷底是土，可走 **0.90×**；峰顶是石，满速，坑半径 **×0.75**。坠入谷是坑（摔/埋），不是虚空。教学：**30°** 从岸越两牙打对岸；**65°** 常削近牙；**90°** 砸近谷可埋。对堂霜丸 / 霆丸同岸略错开（204 / 736），开局不站峰。AI：敌在谷内偏 **90° / 高爆**；对岸 **30°** 越牙；岸唇 **迟雷**。雷泽约 **30%**（不是必开）。落顶仍只洞顶。标题二十一图，随图含齿岸。堂袋 / 堂匣 50% / 八枪 / 洞顶 / 井口 / 余震 / 时尽不改。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

#### 环坑 `ring`（v4.10）

两侧 **土岸**（顶 y≈308，出生 **168 / 772**）中间一座 **干圆坑**（TNT crater-bowl，原创名。不是月池水、不是井口）。沿约 x **480**，沿 y≈**250**，底 y≈**390**，内径约 **140px**，石沿带宽约 **36px**（坑 **×0.72**）。坑底是土，走 **0.55×**。坠入是坑（摔/埋），非虚空、非水，**无**回合浸伤。教学：**30°** 从岸越坑打对岸；**65°** 常削近沿；**90°** 砸坑可埋。对堂霜丸 / 霆丸同岸略错开（204 / 736），开局不站坑、不站沿。AI：敌在坑内偏 **90° / 高爆**；对岸 **30°** 越坑；近沿 **迟雷**。雷泽约 **25%**（不是必开）。落顶仍只洞顶。标题二十二图，随图含环坑。金匣袋 / ×3 分裂 / 0 体开火 / 袋火 / 齿岸 / 洞顶 / 井口 / 月池 / 堂袋 / 堂匣 50% / 八枪不改。overlay `pointer-events: none`。`GRAV` 260、`VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

---

## 11. 模式

标题页按钮（v4.10）：

```
烬丸：[ 易 ] [ 中 ] [ 狠 ]
[ 弹堂 ] [ 堂核 ] [ 演习场 ] [ 对坐 ] [ 对堂 ] [ 堂座 ]
地图： [平原] [峡谷] [双台] [风柱] [碎桥] [悬岛] [残垣] [风谷] [熔台] [廊桥] [双塔] [月池] [断崖] [沙脊] [石门] [霜泽] [云台] [镜廊] [井口] [洞顶] [齿岸] [环坑]
```

键：1 / Enter / 空格 = 弹堂，2 = 堂核，3 = 演习场，4 = 对坐，5 = 对堂，6 = 堂座。对局中途不换模式。对局里 4 仍是三裂，5 仍是霓轨，6 仍是霓火，7 是叠珠，8 是迟雷。

### 11.1 弹堂（1v1）

标准。HP 100。风 −8～+8。伤害 ×1。先手岚丸。有 AI。有道具（v0.2）、有殿破（v0.2）。这是默认。

### 11.2 堂核

HP 60。风 −14～+14，55% 概率直接刮 8–14 的大风。伤害 ×1.15。局更快。道具次数 **不减**。AI 误差更小（§12）。

### 11.3 演习场（v0.2 必须做）

- **无 AI**。烬丸换成木桩「石俑」：HP 9999，不走、不打、不攒怒、不使用道具。位置在该图烬丸出生点，可被打飞/挖摔/坠出（坠出 1.2s 后在出生点重生，飘字「归位」）。
- 岚丸无限回合：settle 完 `beginTurn('p')`，不换手。`turns` 仍 +1，用来练「第 N 发」。
- 风仍每回合重掷。标题页在演习场可加一个小钮「锁风 0 / 锁风 →4 / 随机」（写入 `playbox-tan-tang-best.drillWind`：`rand|0|4l|4r`）。默认随机。
- **右侧或底侧常驻 65° 表**：20 行，当前水平格那一行高亮。旁白三行：
  - `距 N.N 格  Δh ±M.M 格  风_along W`
  - `65° 建议  角 A°  力 P`（已含风和高度）
  - `90° 建议  角 B°  力 95`
- 演习场 **画淡色真实预判弧**（含风含重力，步进与 `traceShot` 相同，最多 180 点，alpha 0.35）。这是唯一允许完整预判线的模式。对局（弹堂/堂核）仍然只画 80px 虚线。
- 不记连胜、不改 `winStreak`。最快不写。结束层不出现（石俑不死）。R 重置地形与次数与怒气。
- 底栏：「演习 · 表随距离变 · 3 回标题」。
- 标题页 3 或按钮「演习场」。`kind='drill'`。`G.kind` 不要复用 `hall/core` 的伤害和风逻辑：演习伤害 ×1，风默认按弹堂范围，除非锁风。

### 11.4 对坐（v0.6，已上）

本机两人，同一套键，把键盘递给对面。`kind='seat'`。HP / 风 / 伤害同弹堂。无 AI、无延迟条、无联网。岚丸永远先手，settle 完换烬丸，再换回来。

- 标题钮「对坐」或键 **4**（仅标题页）。R 重开仍是对坐同图。
- HUD 常显「岚丸的回合」/「烬丸的回合」。顶栏体 / 怒 / 道具 / 辅助 / 预判弧只服务 **当前** 行动者。
- 双方各自一份飞步 / 影挪 / 霓弹 / 鼓息 / 怒气 / 殿破。辅助 0–3 与弹堂相同，只画当前人的弧。
- 弹落地 settle 后镜头缓到下一名射手，不回全场框。
- 胜：吐司与结束层写「岚丸胜」或「烬丸胜」。对坠仍「对坠」。不改连胜 / 最快。
- 不是 2v2，不要霜丸 / 霆丸。

### 11.5 对堂（v0.9，已上；v1.2 左队双控）

四人：岚丸（玩家）+ 霜丸（玩家）vs 烬丸 + 霆丸（皆 AI）。`kind='duo'`。HP / 风 / 伤害同弹堂。延迟见 §6.5。标题钮「对堂」或键 **5**（仅标题页）。对坐 ≠ 对堂。

- 开局 delay：岚丸 0、烬丸 8、霜丸 16、霆丸 24。下一名 = 存活单位里 delay 最低者（并列按此序）。
- 射击落地后 `delay += 100 + 武器额外`（高爆 +30、穿透 +10、三裂 +20、霓轨 +40、霓火 +25、叠珠 +35、霓弹共 90、殿破再 +20）。跳过 / 冻结 +80。飞步 / 影挪 / 鼓息 / 殿破点亮 +0 且不结束回合。
- **左队双控（v1.2）**：轮到岚丸或霜丸时玩家寄身该单位，无 AI。霜丸不跑求解器。右队仍 AI。延迟队列自动切人，无 T 键。
- HUD：寄身岚丸青焰、霜丸冰焰。吐司「岚丸出手」/「霜丸出手」。顶栏体 / 怒 / 道具 / 辅助只服务当前寄身。
- HUD 延迟数字 + 顺序点。倒地的人从队列摘掉。对方两人都 HP≤0 才胜。
- 出生：每侧两人错开约 36px；熔台等窄台仍落在垫上，必要时叠台。
- 殿果与弹堂相同。连胜 / 最快照记。

### 11.6 堂座（v1.6）

本机两人 2v2 热座。`kind='quad'`。规则、出生、延迟、殿果、障幕与 **对堂** 相同，但 **无 AI**：玩家甲寄身左队（岚丸+霜丸），把键盘递给玩家乙后寄身右队（烬丸+霆丸）。标题钮「堂座」或键 **6**（仅标题页）。对堂仍是 PvE。

- 开局 delay 同 §6.5。延迟最低者出手；并列按岚丸 / 烬丸 / 霜丸 / 霆丸。
- 换边（左队 ↔ 右队）时大吐司「把键盘给烬丸」或「把键盘给岚丸」。按 **边** 写队长名，不写霜丸 / 霆丸。同边切人仍「霜丸出手」等。
- 辅助 0–3 只服务 **当前寄身**（谁出手画谁的弧）。道具 / 体 / 怒跟当前单位。
- 胜：不改连胜。结束层「岚丸胜」/「烬丸胜」（边）。对坠仍「对坠」。
- 不是对坐（对坐是 1v1 两人）。不是对堂（对堂右队 AI）。

---

## 12. AI（烬丸）

必须真会打，不乱抖。v0.1 已有求解器，v0.2 只在外围加道具与埋人。

### 12.1 求解器（已上架，保持）

输入：当前风、己方位置、对方位置、高度图、当前武器。

1. 粗搜：角 15–165 步长 3°，力 18–100 步长 4，`traceShot`（含风含武器含穿透）。
2. 精搜：最佳附近角 ±3 步长 1、力 ±4 步长 1。
3. 打分 `scoreImpact`：

| 结果 | 分 |
|---|---|
| 直击对方 | `12000 - t*40`（飞得越快越好） |
| 进溅射半径 | `5000 - d*28` |
| 砸脚 40px 内 | `1800 - feet*16` |
| 其他 | `200 - d*0.45 - mid*0.05` |

4. 走位搜索：试 `x ±40、±72`（夹 28–932），每点重 solve。若新分 > 旧分 +80，采用该走位（受 96px 步数限制）。v2.9：若场上堂匣比敌人近、自己 HP≥40、且本发不是直击（分≥10000）或残血溅射杀，朝匣再多走 36px。绝不放弃击杀去追匣。
5. 误差（在最佳上再抖）：

| 模式 | 角抖 | 力抖 |
|---|---|---|
| 堂核 | ±1.5° | ±2 |
| 弹堂 | ±3° | ±4 |
| 演习场 | 无 AI | — |

v0.1 代码里堂核是 `loose=0`（±1.5/±2），弹堂 `loose=1`（±3/±4）。DESIGN 写过 ±3/±4 与 ±6/±8——**以代码为准**（更准一点）。不要改回更笨。

选枪 `pickAIWeapon`（已上架）：

- 对方埋了或坐在坑里 → **叠珠**（第二发收尾）
- 石门廊 / 沙脊鞍、敌将走进（非开局） → **迟雷** 封路
- 霜泽：敌在湖上近岸唇 → **迟雷** 封退；湖心偏 **65° / 90°** 砸冰（高爆，不跳）
- 井口：敌在井内 → **90° / 高爆**；近出口 → **迟雷** 封爬；对岸偏 **65°**
- 洞顶：敌在牙下 → **90° / 高爆**；对岸 → **30°** 越坑；岸唇 → **迟雷**
- 镜廊：敌在对岸，折弹更近 → **40–55°** 瞄墙；否则 **65°** 越墙
- 峡谷 → 穿透
- 对方左右 36px 地面比脚下低 >28，或对方 x<70 或 x>890 → 高爆
- 否则普通弹

### 12.2 演出（已上架，保持）

不要瞬发。阶段：

0. 等 0.28s  
1. 走到 `walkTo`，78 px/s，吃步数  
2. 转角 70°/s  
3. 可见蓄力，拉到目标力再 `fire`

玩家必须能看见烬丸的蓄力条。

### 12.3 道具使用顺序（v0.2）

`startAI` 里按固定顺序，每项最多一次：

```
1. 若该用鼓息（§8.5）→ 立刻用
2. 若该用影挪（§8.3）→ 用，更新坐标，再 solve
3. 否则若该用飞步（§8.2）→ 用，等 0.34s 落地，再走剩余步
4. 若该用逆息 → 用，再 solve
5. 若该用障幕（§8.8：HP<40 且 |距|<8 格）→ 用，停在雾里
6. 选枪；若该用霓弹（§8.4）→ 装备霓弹
7. 若怒气 100 且命中分≥5000 → 殿破
8. 走 → 转角 → 蓄力 → 打
```

AI 不吃时尽钟，只用自己的思考延迟。不要给烬丸接玩家那条 18s。

### 12.4 埋人战术（v0.2 加强，v0.1 只有砸脚分）

在 `scoreImpact` **之外** 加一项，v0.2 必须加：

```
bury = 0
若 爆心 x 距对方脚 < 34 且 wep.crater >= 30：
    depth = 预计坑深 ≈ wep.crater * 0.7
    bury += 900 + depth * 8
若 对方已在坑（左右 48px 均高 − 脚下高 > 28）且本次能打中坑壁或坑底：
    bury += 1400
score += bury
```

行为口诀：

1. 打不准直击时，高爆砸脚比远空更优（分会抬上去）。
2. 对方进坑后，下一发改穿透或普通弹打坑底，不要继续只刮边。
3. 碎桥图（以后）：站在桥上的人，AI 砸桥中点，分额外 +800。
4. 不要自杀式挖自己脚下。若最佳爆心距自己 < 自己.splash*0.45 且自己 HP≤该溅射，弃选，用次优。

### 12.5 烬丸难度三档（v1.8）

标题页 **易 / 中 / 狠**，写入 `playbox-tan-tang-best.ai`（0 / 1 / 2，缺省 **1 中**）。HUD 常显 `烬 · 易|中|狠`。对局中途不换档。R 重开仍用当前档。

堂座 / 对坐 / 演习场无 AI，档位不生效（标题仍可改，留给下一局弹堂 / 堂核 / 对堂）。对堂右队烬丸 + 霆丸都吃这档。

| 档 | 角抖 | 力抖 | 术 | 殿破 | 障幕 |
|---|---|---|---|---|---|
| **易** | ±8° | ±6 | 约 18% 才用；埋了仍可影挪/飞步逃 | 仅自己 HP 低于 30 且命中分≥5000 | **从不** |
| **中** | 弹堂 ±3° / ±4；堂核 ±1.5° / ±2；65° 再力抖 5–8% | 同左 | 现人味（§12.3） | 怒 100 且分≥5000 | HP 低于 40 且距小于 8 格 |
| **狠** | ±2° | ±2 | 贪：浅坑即三裂埋；逆息风≥2 且翻转能加分就用；对堂用普通/穿透/三裂抢低延迟两手（直击不让） | 怒 100 且分≥1500 | HP 低于 70 或距小于 12 格 |

雾内误差 +12% 三档都吃（障幕规则，不是难度）。不要写传送 / 飞行 / 三叉戟 / 激怒。

v4.3 堂袋武装：狠 — 敌暴露且体≥60 → ×2 再 +3（无 +3 则 +2）；|距| 小且体≥80 → 偏 ×3++2。永不叠 ×2 与 ×3。中 — 只 ×2，无 plus。易几乎不用。HP<40 仍可回春。

---

## 13. 进度路线

### 13.1 本周必须做完 = v0.2

目标：对局里能「走、跳、挪、冻、攒怒、开殿破」，并能在演习场对着表练 65°。不做新图、不做新枪、不做 2v2、不做小地图。

| # | 项 | 验收 |
|---|---|---|
| 1 | **十格尺** | 48px 一根，当前格高亮，Tab 开关，写入 best.ruler |
| 2 | **体力** | 回合 100；走 1px=1；走仍最多 96；HUD「体」 |
| 3 | **飞步** | Q，−35，水平 80，弧 56，0.34s，每局 2，落地可摔 |
| 4 | **影挪** | E，−25，圆 220，点地确认，每局 1 |
| 5 | **霓弹** | C，普通弹道，直击 8，命中冻 1 回合，每局 2 |
| 6 | **鼓息** | V，+50 怒，每局 1 |
| 7 | **怒气 + 殿破** | 伤害公式见 §6.3 / §9，F 点亮，下一发 ×1.6、坑 ×1.35 |
| 8 | **跳过** | X，aim 才能过 |
| 9 | **上回残影** | 淡金虚线 + `上 角/力` |
| 10 | **演习场** | 无 AI，石俑，锁风可选，常驻 65° 表 + 预判弧 |
| 11 | 道具条 + 触屏 | QECV 与条同步 |
| 12 | AI 会用四术和殿破 | 按 §12.3，能看见烬丸先鼓息再打 |
| 13 | 文档 | 只改 GDD；`node --check js/game.js` 仍过 |

v0.2 **不要**：三裂/霓轨/霓火、风柱/碎桥/悬岛、小地图、暴击、2v2、CDN、新依赖。

### 13.2 v0.3

已上：三裂、风柱、碎桥、镜头跟弹 + 命中 punch、埋了、瞄准建议行。

### 13.2b v0.4

已上：殿塌、烬丸人味、拖尾/着土/HP 弹出/直击慢镜/殿破更响、霓轨、悬岛。辅助 0–3 不改。

未做（以后）：小地图 160×90、格尺在风柱带内变色、结束层回放。

### 13.2c v0.5

已上：霓火、残垣、超时钟末 5 秒脉动、殿塌碎屑落地扬尘。辅助 0–3、65° 建议、跟弹镜头不改。

### 13.2d v0.6

已上：本机对坐（标题 4）。两人轮流，无 AI、无联网。HUD 写谁的回合；双方道具/体/怒/辅助；落地镜头缓到下一名；胜吐司写名字。

### 13.2e v0.8

已上：弹道中 **殿果**（弹堂 / 堂核 / 对坐 / 对堂）。每发 0–2 颗，开局岚丸第一回合不刷，演习场不刷。弹心碰到随机 +1 飞步 / 影挪 / 霓弹 / 鼓息（上限开局 +1），约 15% 金果一次 +25 怒。未击中的果 settle 后消失。烬丸弹道碰到即拾，不找果。辅助 中 / 强落点轻脉动。

### 13.2f v1.2

已上：对堂左队双控。延迟队列切人，岚丸与霜丸皆玩家，烬丸与霆丸仍 AI。HUD 寄身青/冰。吐司「岚丸出手」/「霜丸出手」。无 T 换人。

### 13.2g v1.4

已上：**障幕**（G，每局 1，−20 体，半径 70）。下一次敌方行动后消散；对堂队友不消耗。雾内藏对方辅助落点、AI 误差 +12%、自己仍可见。AI：HP<40 且 |距|<8 格。不进殿果。

### 13.2h v2.0

已上：**上回弹道残影**。任意一发落地记全长折线；下一单位瞄准（所有人）画金色 30% 虚线 + HUD「上 角/力」；飞弹期间不画；只活本局；K 开关，默认开，`best.ghost`。十二图、易中狠、堂座、障幕、殿果、辅助不改。

### 13.2i v2.1

已上：新图 **断崖**。左高右低、中段约 180px 绝壁；90° 砸滩 / 30° 掏壁。坠崖摔伤非即死；崖底浅水每回合 2。标题十三图，随图含断崖。上回残影 K 不改。

### 13.2j v2.2

已上：**风丝**。天空横向丝随 `|风|` 变密变快，方向跟号；`|风|≥5` 更粗更快。近地叶尘可选。减动效藏丝，顶栏数字仍在。不改物理。断崖与上回残影 K 不改。

### 13.2k v2.3

已上：新图 **沙脊**。两侧高丘、中央浅鞍；出生 168 / 772。沙地走 0.55×，坑 ×1.25。90° 砸鞍 / 30° 掏对丘。风掷 ±1 仍封 `windMax()`。对堂额外同丘。标题十四图，随图含沙脊。风丝、残影 K、断崖、月池不改。

### 13.2l v2.4

已上：**叠珠**（键 7）。一发两壳，0.38s / 0.72× 力 / ±1.2° / 第二发溅射坑 ×0.72。不能第三发；第一发结束对局不刷第二发。AI 埋了/坑里用。HUD「叠珠」「两发」。十四图、风丝、K 残影、断崖、沙脊不改。`GRAV` 260、`VK` 420。

### 13.2m v2.5

已上：**雷泽**。风谷/断崖/沙脊必开，熔台永不开，其余约 35%。画布闪雷 + 雨丝；8–14s 风可 ±1 仍封顶；湿沙/草走 ×0.88；飞步不变。HUD「雷泽」不改图名。减动效藏闪与雨留芯片。叠珠 / 十四图 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。

### 13.2n v2.6

已上：新图 **石门**。两柱（约 x 280 / 660）+ 中缝廊；出生 168 / 772 外沿。30° 穿缝 / 90° 砸冠 / 65° 易削沿。柱石坑 ×0.72（x 带 + 图）。冠上满速，坠入廊可埋。对堂额外同外沿。AI 廊里 90°/高爆，对冠 65°。雷泽约 35%。标题十五图。叠珠 / 风丝 / K 残影 / 断崖 / 沙脊不改。`GRAV` 260、`VK` 420。

### 13.2o v2.7

已上：**迟雷**（键 8）。着地 1.6s 后爆，溅射 42 / 坑 34；碰人立刻 ×0.85 溅射。每边一枚，再打旧雷熄。AI 封石门廊 / 沙脊鞍，不开局。十五图 / 雷泽 / 叠珠 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。

### 13.2p v2.8

已上：新图 **霜泽**。两侧土岸 + 中央宽平冻湖；出生 168 / 772。冰走 1.15× 且滑；浅着角 <28° 跳一次再爆；冰坑 ×0.55。对堂同岸。AI 65°/90° 砸湖、岸唇迟雷。雷泽约 35%。标题十六图。叠珠 / 迟雷 / 风丝 / K 残影 / 断崖 / 沙脊 / 石门不改。`GRAV` 260、`VK` 420。

### 13.2q v2.9

已上：**堂匣**。回合间 0–1 木匣落可走土面，场上最多 2。直击/溅射/走进拾。十六图 / 叠珠 / 迟雷不改。`GRAV` 260、`VK` 420。

### 13.2r v3.0

已上：**时尽**。瞄准钟 18s / 堂核 14s / 殿塌 11s。末 5s 脉动 + 可选 tick，减动效只改色。到 0 等同 X 过，toast「时尽」，无半力白给。overlay / 飞弹 / 迟雷引信停钟。AI 不吃这时钟。不另做第二只钟。十六图 / 叠珠 / 迟雷 / 堂匣不改。`GRAV` 260、`VK` 420。

### 13.2s v3.1

已上：新图 **云台**。两侧土岸 + 中央滑动石板（约 220px，振幅 90、周期 7s）。站板吃 vx；板坑 ×0.8，挖穿停成残石；坠入缝摔/埋非虚空。对堂额外在岸。AI 板上提前量 vx×tof，偏 65°。雷泽约 30%。十七图 / 叠珠 / 迟雷 / 堂匣 / 时尽 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。

### 13.2t v3.2

已上：新图 **镜廊**。两侧土岸 + 中央浅沟镜墙（约 18×220）。侧击 vx×−1 折一次再爆；顶砸啃石 ×0.5；墙矮/柱少塌成残石不再折。走进实心，坠沟可埋。对堂额外在岸。AI 对岸优先 40–55° 折近，否则 65° 越墙。雷泽约 30%。十八图 / 云台 / 时尽 / 堂匣 / 迟雷 / 叠珠 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。

### 13.2u v3.3

已上：**余震**。坑 ≥36 或高爆/三裂/迟雷爆后 0.28s：3–5px 微震（减动效不抖）、沿屑 4–7、薄沿 50% 滑 8–14px。只一道，二次刷新。无吐司。顿帧仍 140ms。十八图 / 八枪 / 堂匣 / 时尽 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。

### 13.2v v3.4

已上：新图 **井口**。两侧土岸 + 中央约 240px 深井（底有暗水盘）。90° 砸井 / 30° 越唇 / 65° 削近唇。井内走 0.40×、回合浸 4。爬出砸坡或飞步。石唇坑 ×0.75，底泥 ×1.15。对堂额外在岸。AI 井内 90°/高爆、出口迟雷，对岸 65°。雷泽约 30%。大坑仍吃余震。十九图 / 云台 / 镜廊 / 时尽 / 堂匣 / 迟雷 / 叠珠 / 风丝 / K 残影不改。`GRAV` 260、`VK` 420。

### 13.2w v3.7

已上：**堂袋 polish**。图标 40 viewBox 宝石卡；格 56×72 + 珐琅点；武装金斜面/火花；芯片「巽+沉+重」；空格 0.12s 晃；堂匣吐司跟图标同色；瞄准力旁「袋」。七物 / 十九图 / `GRAV` 260 / `VK` 420 不改。

### 13.2x v4.0

已上：**堂袋重做**。底栏 ×2 / ×3 / +1 / +2 / +3 / +5 / 回春。×2×3 互斥；+N 叠加。每壳 `(x2?0.90:x3?0.60:1)*plusMul`。开火扣体，体不够吐司「体不够」。叠珠仍是枪 7。术仍在顶栏。十九图 / `GRAV` 260 / `VK` 420。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

### 13.2y v4.1

已上：**堂袋 glyph polish + use juice**。7 枚厚描边宝石卡（×2 金 / ×3 绯红 / +1 奶油 / +2 橙 / +3 猩红 / +5 金箔 / 回春叶）。芯片同色。开火叠物：炮口混色喷（减动效关）+ 格闪 0.18s。叠算 / 体 / 互斥 / 十九图 / 八枪 / `GRAV` 260 / `VK` 420 不改。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

### 13.2z v4.2

已上：**堂袋 stack readout**。瞄准力旁 `2发×1.35`；仅 +N 时 `+50%` / ×1.50。体不够标红，吐司「体不够」仍在。芯片 ×2++3 保留。叠算 / 体 / 十九图 / 八枪 / `GRAV` 260 / `VK` 420 不改。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

### 13.2aa v4.3

已上：**堂匣 drops ×/+ more, AI combos**。堂匣非金袋物 **50%**；抽袋偏 ×2 / +3 / +2。吐司「堂匣 · ×2」跟 `BAG_TINT`。狠：暴露且体≥60 叠 ×2++3（无 +3 则 +2）；近距且体≥80 偏 ×3++2；永不叠 ×2 与 ×3。中只 ×2。易几乎不用。叠算 / 体 / 十九图 / 八枪 / `GRAV` 260 / `VK` 420 不改。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

### 13.2ab v4.4

已上：新图 **洞顶**。两侧土岸 + 中央浅坑 + 洞顶钟乳。90° 砸顶 / 65° 削牙 / 30° 越坑。牙下高爆、对岸 30°、岸唇迟雷。洞顶坑 ×0.70。雷泽约 20%。标题二十图。堂袋 / 堂匣 50% / 八枪 / `GRAV` 260 / `VK` 420 不改。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

### 13.2ac v4.5

已上：**落顶**。洞顶 / 牙真正啃石后落 2–4 粒石屑（重力、无风、~4px）。0.55s 内碰人 3 HP（堂核 ×1.15），每人每爆一粒；碰地消失；减动效不刷伤害石屑。不是第九把枪。只洞顶。AI 不瞄准。余震 / 堂袋 ×2×3 +N / HUD `2发×1.35` / 堂匣 50% / 八枪 / 二十图 / `GRAV` 260 / `VK` 420 不改。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

### 13.2ad v4.6

已上：新图 **齿岸**。两侧土岸 + 四座石牙。30° 越两牙 / 65° 削近牙 / 90° 砸近谷。谷走 0.90×，峰坑 ×0.75。谷内高爆、对岸 30°、岸唇迟雷。雷泽约 30%。标题二十一图。落顶 / 洞顶 / 堂袋 / 堂匣 50% / 八枪 / `GRAV` 260 / `VK` 420 不改。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

### 13.2ae v4.7

已上：**袋火**。×2 / ×3 齐射每壳短拖尾（金 `#ffe36b` / 绯红 `#dc143c`）；叠 +N 屑混色；后续发略亮。减动效跳过拖尾，炮口喷仍跳过。不是第九把枪。叠算 / 体 / 二十一图 / 八枪 / 落顶 / HUD `2发×1.35` / 堂匣 50% / `GRAV` 260 / `VK` 420 不改。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。


### 13.2af v4.8

已上：×3 **同时分裂**。一发三壳同膛出门，角 ±8°（夹 0–180），每壳 ×0.60，不再 0.32s 连打。×2 仍后发。叠珠+×3 只裂本发。名字仍 ×3。体不够卸武装，普通弹照打。袋火 / HUD `3发×0.60` / 二十一图 / 八枪 / 落顶 / `GRAV` 260 / `VK` 420 不改。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

### 13.2ag v4.10

已上：新图 **环坑**。两侧土岸 + 中央干圆坑。30° 越坑 / 65° 削近沿 / 90° 砸坑。坑走 0.55×，石沿坑 ×0.72。坠入摔/埋，非水无浸。坑内高爆、对岸 30°、近沿迟雷。雷泽约 25%。标题二十二图。金匣袋 / ×3 分裂 / 0 体开火 / 袋火 / 落顶 / 齿岸 / 八枪 / `GRAV` 260 / `VK` 420 不改。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

### 13.2ah v4.11

已上：**蓄缓**。蓄力 2s 走满 100（原 0.8s）。堂袋 +N 仍可叠。`GRAV` 260 / `VK` 420 不改。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

### 13.2ai v4.12

已上：**蓄条**。蓄力时力旁短模拟条从 TAP_POW 填到 100，数字仍更新。满档金闪一次仍可松。减动效只填不闪。标题 overlay 藏。`pointer-events: none`。不是第九把枪。`CHARGE_T` 仍 2。堂袋叠算 / 二十二图 / 八枪 / `GRAV` 260 / `VK` 420 不改。禁止写传送 / 飞行 / 三叉戟 / 激怒 / 天使 / 恶魔。

### 13.3 v1.0

- 2v2「对堂」+ 延迟条
- 暴击 8%
- 同伴霜丸 / 霆丸独立立绘
- 仍无联网、无 CDN、无账号

### 13.4 明确永远不做

移动射击、即时制、第三人称、地上随机掉落物（殿果只在弹飞行中出现，不掉地上）、内购、账号、排行榜联网、暂停菜单、悔弹、手柄、第三人加入、从别的弹道游戏抄名字。

---

## 14. 技术约束

### 14.1 架构


单页三文件：index.html、css/style.css、js/game.js。v0.2 不许拆新脚本、不许打包器。
无 CDN、无外部字体、无外部音效。Web Audio 现造。
60fps Canvas，逻辑步每秒 60 次，页面隐藏时停。
输入零黏滞，松手立刻停走停调角。
语法检查必须通过。无界面时跑 selfCheck。v0.2 给格尺、冻结、殿破各加一条，不删旧条。
g/tan-tang/index.html 单独打开就能玩。

### 14.2 本地存储键

只许两个键名。新字段塞进 best 的 JSON，不要再开第三键。

playbox-tan-tang-best：JSON，字段 winStreak、bestTurns、mode（hall 或 core）、map（plain/canyon/twin，v0.3 加新 id）。v0.2 加 ruler（布尔，缺省真）、drillWind（rand 或 0 或 4l 或 4r）。v1.8 加 **ai**（0 易 / 1 中 / 2 狠，缺省 1）。v2.0 加 **ghost**（布尔，缺省真；只存开关，不存路径）。
playbox-tan-tang-mute：字符串 1 表示静音。

读写全部包住以免无存储环境报错。不存盘面、不存血量、不存道具次数、不存怒气。
演习场不更新连胜与最快。mode 不要写成 drill。

### 14.3 弹层点击穿透

overlay 与 overlay.hidden 均为 pointer-events: none。
只有面板按钮可点。
toast、扫描线、格尺、残影、风丝、雷泽雨丝/闪雷、余震沿屑与边尘、袋火拖尾与屑、蓄条、小地图、演习场表全部不可点。
道具条按钮、顶栏按钮、虚拟键是唯一新增可点区域。
绝不做第二个全屏吃点击的层。结束层不挡顶栏重开与静音。R 在弹层打开时仍可用。

### 14.4 输入与无障碍

- html、body、canvas 均 touch-action: none。
- 方向键与空格阻止默认滚动。
- 第一次按键或点击后才恢复音频上下文。
- 系统减少动态效果时：缩短顿帧、少粒子、不抖屏。格尺仍画。飞步位移仍发生（那是规则不是特效）。风丝与叶尘隐藏，顶栏风数字仍在。雷泽藏闪雷与雨丝，HUD「雷泽」仍在。时尽末 5 秒只改色、不脉动。余震不抖、少土屑，薄沿滑步仍发生。袋火跳过额外拖尾与屑，炮口喷仍跳过。
- 切走页面或失焦：停蓄力音，清按键。不要在后台把时尽钟扣完。
- 画布 role=application，血条数字同步，按钮有读屏标签。

### 14.5 表现层（已上架，v0.2 沿用）

听：蓄力升调、出膛呼、弹道风哨、直击爆、溅射闷、挖土、摔落、击破。各武器音色不同。殿破加一条更低的正弦下滑。飞步短促上滑，影挪一闪三角，鼓息金和弦，霓弹命中带锁感下行。

看：爆心粒子、土屑、血量数字蹦出、血条闪、弹道拖尾 28 点、击破全屏一闪、连堂次数。v3.3 大坑 **余震**：坑沿土屑 + 画布边尘。

触：命中顿帧直击 **140ms**（v1.7）、溅射较短、殿破 80ms，加镜头 punch。打空不顿帧。余震再加 0.28s、3–5px 微震，不替换顿帧。

颜色锁定：青 0,232,255 岚丸；洋红 255,61,184 烬丸；金 255,227,107 殿破与连堂；紫 139,92,255 辅助。

### 14.6 新增 DOM（v0.2 许可的最小集合）

只许在 index.html 加这些，不许新文件：

- items：道具条，五钮飞步、影挪、霓弹、鼓息、过
- rage-label：怒气数字
- ghost-label：上回角与力
- storm-label：雷泽芯片，未开则隐藏
- drill-table：演习场表，非演习隐藏
- btn-drill：标题页演习场
- drill-wind：锁风钮
- ai-tier：标题页烬丸 易 / 中 / 狠
- ai-label：顶栏「烬 · 中」

格尺和残影画在画布里，不要 DOM。

### 14.7 性能预算

- 高度图 960 列，挖坑按半径扫描。
- AI 粗搜约 1071 条轨迹，每条最多 420 步，必须在回合开始时同步算完。影挪采样若卡则降到 12 点。
- 减少动态效果时一次粒子最多 8；平时单次最多 28。
- 只在挖坑后重绘地形，不要每帧重绘。

### 14.8 禁止事项（技术）

- 禁止远程请求、远程脚本。
- 禁止把密钥或账号写进仓库。
- 禁止在样式里拉外部 https 资源。
- 禁止改 git 配置，禁止未要求时提交。

---

## 15. 验收清单

分两块：v0.1 已上架必须继续为真；v0.2 做完再勾。

### 15.1 v0.1 回归（改 v0.2 时一条都不能坏）

- 平原无风，65 度加力 70，普通弹，水平距离 500 到 700 像素，大约打到对岸开局。
- 同力 45 度比 65 度近至少 8 像素。
- 同力 90 度落点距炮口小于 50 像素。
- 风负 8 与正 8 的落点水平差大于 24 像素，拖尾肉眼弯。
- 高爆坑明显大于普通弹（48 对 30）。
- 穿透能穿峡谷薄壁，只穿一次，0.18 秒或二次碰地才爆。
- 挖掉脚下会掉；落差至少 36 像素扣摔伤，公式 min(28, floor((fall-36)*0.35))，堂核再乘 1.15。
- 掉出画布底血量为 0，飘字坠亡。
- 堂核血量 60、风更大、伤害乘 1.15，一局明显更快。
- 岚丸永远先手。烬丸会走、会转角、会可见蓄力，不瞬发。
- 时尽：瞄准 18s（堂核 14 / 殿塌 11）到 0 等同 X 过，toast「时尽」，无半力白给。
- R 在标题、对局、结束层都能重开；结束层不挡顶栏重开。
- overlay 与 overlay.hidden 均为 pointer-events: none。
- M 静音写入 playbox-tan-tang-mute。连胜、最快、模式、地图写入 playbox-tan-tang-best。
- 命中有声、有粒子、有顿帧；打空不顿帧。
- 连续有效命中蹦连堂次数；打空清零。
- 胜「堂破了」负「落堂了」平「对坠」。
- 语法检查通过；无界面 selfCheck 全过。
- 无 CDN。中文界面。标题「弹堂」加 TAN HALL。
- 全文、界面、注释不出现禁用词：传送、飞行、三叉戟、激怒，以及任何外部角色、武器、地图名。

### 15.2 v0.2 新功能

尺与残影：

- 对局默认画十格尺，间隔 48 像素，从行动者脚算起，左右到边或 20 格。
- 当前口诀格高亮。Tab 开关。刷新后记住 best.ruler。
- 打出一发落地后，下一单位瞄准期出现「上 角度/力度」，画布淡金 30% 虚线重描全长轨迹（不随新风改）。所有人下一手都画。弹在飞时不画。
- 第一回合、重开、换图无残影。K 开关，默认开，写入 `best.ghost`。路径只活本局。

体力与走位：

- 每回合体 100。走路 1 像素耗 1 体，且走路最多 96 像素。
- 体 0 仍能射击、鼓息、殿破、跳过；不能走、不能飞步、不能影挪。
- 界面「体」与数字同步。

四术：

- Q 飞步：耗 35 体，水平 80 像素，弧顶 56 像素，0.34 秒，每局 2。不吃风。断层上可坠。
- E 影挪：进入点地，圆半径 220 像素，确认后耗 25 体，每局 1。ESC 取消不扣。
- C 霓弹：覆盖本发，弹道同普通弹，直击 8、溅射 28、坑 18。命中敌方伤害至少 1 则冻结其下一完整行动（重掷风、停 0.55 秒、不走不打）。每局 2，打出才扣次。
- V 鼓息：怒气加 50（上限 100），每局 1，不占射击。
- 次数用完或体不够：灰钮加提示，不误放。
- 触屏道具条与键同步。过 = X。

怒气与殿破：

- 对敌伤害加 floor(伤害×0.45)，受伤加 floor(伤害×0.55)，夹在 0 到 100。
- 满 100 时 F 或点条：怒气清 0，下一发伤害乘 1.6（在堂核 1.15 之后），坑半径乘 1.35，金闪加飘字「殿破」。
- 点亮后跳过或被冻跳过：强化消失，怒气不退。
- 时尽到 0 等同跳过，殿破浪费，不打出。

跳过与冻结：

- X 仅瞄准阶段可用，不发射换手。蓄力中 X 无效。
- 岚丸被冻时不会走到时尽过，按冻结跳过走。

演习场：

- 标题页按 3 或按钮进入。无烬丸人工智能。石俑血量 9999，不还手，坠出后 1.2 秒归位。
- 只有岚丸回合，落地稳定后回到岚丸。
- 常驻 20 行 65 度表，高亮当前格；三行建议（65 度、90 度、风与高度差）。
- 画淡色完整预判弧（含风含重力）。弹堂与堂核模式没有这条弧，仍只有 80 像素虚线。
- 不改连胜、不改最快。R 重置地形、次数、怒气。
- 锁风 0 / 右 4 / 左 4 / 随机可用，写入 best.drillWind。

人工智能：

- 开回合：该鼓息则鼓息；该影挪则影挪；该飞步则飞步；该霓弹则装备；怒 100 且命中分至少 5000 则殿破；再走、转、蓄、打。
- 能看见烬丸用术，不是瞬移结果。
- 砸脚与埋人分生效：打不准时高爆更常砸脚下。
- 峡谷仍优先穿透；薄台优先高爆。
- 不挖爆自己（溅射会自杀且自己血不够时弃选）。

技术：

- 仍无 CDN、无新依赖、无新脚本文件。
- 弹层点击穿透保持。
- 存储键名不增第三键。
- 语法检查加扩充后的 selfCheck 全过。
- 中文界面。道具名只有飞步、影挪、霓弹、鼓息。必杀只有殿破。

### 15.3 v0.3 与 v1.0 预验收（现在不要做）

- 三裂：落地三坑，直击 14（已上）。
- 霓轨：风加速度随时间加强，弹速系数 0.70（v0.4 已上）。
- 霓火：落地火区半径 28，烧两个对方回合，每跳 8 乘倍率。
- 风柱、碎桥、悬岛出生点与局部风区按第 10.2 节（v0.4 悬岛已上）。
- 小地图 160×90，不可点。
- 两人两人对决延迟表按第 6.5 节，标题页「对堂」。
- 暴击 8% 乘 1.5，可与殿破叠。

### 15.4 手感口诀（给测试的人，不是给代码的）

1. 先报格：脚到脚水平除以 48。
2. 无风同高：65 度查表出力。
3. 顺风把角往 90 推 2 乘风，逆风往 45 推 2 乘风。
4. 近了改 90 度学校：角 = 90 减格再加减 2 乘风，力 95。
5. 高了加力（65 度）或把 90 度再推平一点。
6. 对面贴边或站薄台：高爆砸沿，逼摔。
7. 峡谷隔墙：穿透。
8. 怒满先看能不能打中，再殿破。不要空放。

---

## 附录 A · 单位与换算速查

| 量 | 值 |
|---|---|
| 1 格 | 48 像素 |
| 满屏 | 20 格 = 960 像素 |
| 步行上限 | 96 像素 = 2 格 |
| 飞步水平 | 80 像素 ≈ 1.67 格 |
| 影挪半径 | 220 像素 ≈ 4.58 格 |
| 身体半径 | 14 像素 |
| 直击判定 | 中心距不超过 19 像素 |
| 摔伤起算 | 36 像素 = 0.75 格 |
| 重力 | 260 像素/秒² |
| 风加速度 | 风力 × 2.05 像素/秒² |
| 基准初速 | 420 × (力/100) × 弹速 × loft |
| loft（65 度） | 1.55 |
| 蓄满 | 2 秒（12 到 100） |
| 时尽 | 18s（堂核 14 / 殿塌 11）到 0 过 |
| 顿帧 | 直击 66 / 溅射 48 / 殿破 80 毫秒 |

## 附录 B · 65 度表（再贴一次，给演习场直接抄）

力 = 四舍五入(19.61 × 根号(格))，同高无风普通弹。

20, 28, 34, 39, 44, 48, 52, 55, 59, 62, 65, 68, 71, 73, 76, 78, 81, 83, 85, 88

对应格 1 到 20。风：仰角 = 65 + 2 × 风力 × 朝向。高度：力 += 四舍五入(3 × 高度差格)。

## 附录 C · 名词表（只用这些）

| 用 | 不用 |
|---|---|
| 弹堂 / TAN HALL | 任何外部商标或关卡名 |
| 岚丸、烬丸、霜丸、霆丸、石俑 | 外部角色名 |
| 平原、峡谷、双台、风柱、碎桥、悬岛、残垣、风谷、熔台、廊桥、双塔 | 外部地图名 |
| 普通弹、高爆、穿透、三裂、霓轨、霓火、叠珠 | 外部武器名 |
| 飞步、影挪、霓弹、鼓息 | 飞行、传送、激怒 |
| 殿果（金殿果） | 外部空中道具名 |
| 堂匣（金匣） | 外部地上木箱 / TNT 道具箱原名 |
| 殿破（必杀）、堂破了（胜利） | 外部必杀名 |
| 连堂 | 外部连击名 |
| 堂核、演习场、对堂 | 外部模式名 |
| 落顶、余震、雷泽、时尽 | 外部洞穴落石 / 余震原名 |
| 格、体力、怒气、延迟 | — |

---

文档版本：对应已上架 v0.1–v0.4。实现时以 `js/game.js` 数字为准；改手感先改脚本常量再回写本文。
